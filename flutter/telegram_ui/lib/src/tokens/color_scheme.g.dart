// GENERATED CODE - DO NOT MODIFY BY HAND.
//
// Generated by flutter/tool/emit_dart.py from flutter/tool/theme_tokens.json
// (token codegen pipeline stages 2-3, flutter/docs/ARCHITECTURE.md section 4.3).
// Regenerate: python3 flutter/tool/gen_tokens.py && python3 flutter/tool/emit_dart.py
// Drift gate:  both scripts support --check (byte-compare, nonzero exit).
//
// Inputs:
//   theme_tokens.json sha256: 3637b188b1b39cb7514f967e29e67177a2d921414b2413da49a76b4813f489d7
//   groups.toml       sha256: 824adc4fff5d5fc527efc558472efc9ba23fa10913d8ca2b6066ecd43a85c277
// Upstream Java sources (paths + sha256 recorded in theme_tokens.json meta):
//   Theme.java: 12edf137d084f44989269d8f5914d17055cc885b307db5e9de8ab5acdf1a84ef
//   ThemeColors.java: 357f9eaf6b0eccd1924344bd60f33e3a0cdedced88025de2edf0e50f2350c362
//   bluebubbles.attheme: b1829c34447c8b0db06477c99d8c41f15a50c0db8b2fa8d3e4f693c29e88f69b
//   darkblue.attheme: 6922327115c412ef24cacc21af0546cd23dccab6e1f94e320a51332eed344bc0
//   arctic.attheme: 7e2ea3968b982b49b3eb5b9e030d88ba30806a1cb23c477e6e7b94397e1c6c74
//   day.attheme: a90cad2c5914a181db8500f63f39303782157694098a2ed81744345a334152d3
//   night.attheme: 8f0ab17a18ec6bdcba4907bf869acfcd985ebce350d6854befbbb2089fd57978
//
// Output is dartfmt-clean by construction: `dart format` must be a no-op
// on this file (enforced in CI with `dart format --set-exit-if-changed`).

import 'dart:ui' show Color;

import 'theme_keys.g.dart';

/// Resolves a [TelegramColorKey] ordinal into a themed [Color] (the
/// Theme.getColor analog supplied by the theme runtime).
typedef TelegramColorResolver = Color Function(int key);

/// Typed accessor groups over the flat token space, generated from
/// flutter/tool/groups.toml. Example: `scheme.chat.inBubble` instead of
/// `resolve(TelegramColorKey.chat_inBubble)`. Keys outside every group are
/// reachable through [ungrouped] (and always through [resolve]).
class TelegramColorScheme {
  const TelegramColorScheme(this.resolve);

  /// The underlying resolver — the full-key-space escape hatch.
  final TelegramColorResolver resolve;

  /// windowBackground* — window / page background and on-background text
  WindowColors get window => WindowColors(resolve);

  /// dialog* — alert dialog surfaces, text and controls
  DialogColors get dialog => DialogColors(resolve);

  /// actionBar* — action bar chrome, tabs, submenus
  ActionBarColors get actionBar => ActionBarColors(resolve);

  /// chats_* — the dialogs (chat list) screen
  DialogsColors get chats => DialogsColors(resolve);

  /// chat_* — chat screen: bubbles, service messages, input panel
  ChatColors get chat => ChatColors(resolve);

  /// avatar_* — avatar fills and action-bar tints
  AvatarColors get avatar => AvatarColors(resolve);

  /// voipgroup_* — group calls UI
  VoipGroupColors get voipgroup => VoipGroupColors(resolve);

  /// profile_* — profile screen
  ProfileColors get profile => ProfileColors(resolve);

  /// stories_* — stories viewer and circles
  StoriesColors get stories => StoriesColors(resolve);

  /// premium* — premium promo gradients and buttons
  PremiumColors get premium => PremiumColors(resolve);

  /// statisticChart* — statistics charts
  StatisticColors get statistic => StatisticColors(resolve);

  /// location_* — location sharing UI
  LocationColors get location => LocationColors(resolve);

  /// player_* — full-screen audio player
  PlayerColors get player => PlayerColors(resolve);

  /// inappPlayer* — the in-app mini player bar
  InAppPlayerColors get inappPlayer => InAppPlayerColors(resolve);

  /// switch* — switch tracks and thumbs
  SwitchColors get switches => SwitchColors(resolve);

  /// checkbox* — checkboxes (round and square)
  CheckboxColors get checkbox => CheckboxColors(resolve);

  /// featuredStickers_* — featured sticker pack cells
  FeaturedStickersColors get featuredStickers =>
      FeaturedStickersColors(resolve);

  /// groupcreate_* — group create / invite screens
  GroupCreateColors get groupcreate => GroupCreateColors(resolve);

  /// code_* — syntax highlighting in code blocks
  CodeColors get code => CodeColors(resolve);

  /// color_* — peer name color palette
  PeerColors get peerColors => PeerColors(resolve);

  /// glass_* — liquid glass tab bar and surface tint targets
  GlassColors get glass => GlassColors(resolve);

  /// telegram_color* — the Telegram accent color family
  AccentColors get accent => AccentColors(resolve);

  /// Catch-all for keys not covered by any group in groups.toml.
  UngroupedColors get ungrouped => UngroupedColors(resolve);
}

/// windowBackground* — window / page background and on-background text
class WindowColors {
  const WindowColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_windowBackgroundWhite
  Color get white => _resolve(TelegramColorKey.windowBackgroundWhite);

  /// key_windowBackgroundUnchecked
  Color get unchecked => _resolve(TelegramColorKey.windowBackgroundUnchecked);

  /// key_windowBackgroundChecked
  Color get checked => _resolve(TelegramColorKey.windowBackgroundChecked);

  /// key_windowBackgroundCheckText
  Color get checkText => _resolve(TelegramColorKey.windowBackgroundCheckText);

  /// key_windowBackgroundWhiteInputField
  Color get whiteInputField =>
      _resolve(TelegramColorKey.windowBackgroundWhiteInputField);

  /// key_windowBackgroundWhiteInputFieldActivated
  Color get whiteInputFieldActivated =>
      _resolve(TelegramColorKey.windowBackgroundWhiteInputFieldActivated);

  /// key_windowBackgroundWhiteGrayIcon
  Color get whiteGrayIcon =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayIcon);

  /// key_windowBackgroundWhiteBlueText
  Color get whiteBlueText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText);

  /// key_windowBackgroundWhiteBlueText2
  Color get whiteBlueText2 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText2);

  /// key_windowBackgroundWhiteBlueText3
  Color get whiteBlueText3 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText3);

  /// key_windowBackgroundWhiteBlueText4
  Color get whiteBlueText4 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText4);

  /// key_windowBackgroundWhiteBlueText5
  Color get whiteBlueText5 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText5);

  /// key_windowBackgroundWhiteBlueText6
  Color get whiteBlueText6 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText6);

  /// key_windowBackgroundWhiteBlueText7
  Color get whiteBlueText7 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueText7);

  /// key_windowBackgroundWhiteBlueButton
  Color get whiteBlueButton =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueButton);

  /// key_windowBackgroundWhiteBlueIcon
  Color get whiteBlueIcon =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueIcon);

  /// key_windowBackgroundWhiteGreenText
  Color get whiteGreenText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGreenText);

  /// key_windowBackgroundWhiteGreenText2
  Color get whiteGreenText2 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGreenText2);

  /// key_windowBackgroundWhiteGrayText
  Color get whiteGrayText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText);

  /// key_windowBackgroundWhiteGrayText2
  Color get whiteGrayText2 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText2);

  /// key_windowBackgroundWhiteGrayText3
  Color get whiteGrayText3 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText3);

  /// key_windowBackgroundWhiteGrayText4
  Color get whiteGrayText4 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText4);

  /// key_windowBackgroundWhiteGrayText5
  Color get whiteGrayText5 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText5);

  /// key_windowBackgroundWhiteGrayText6
  Color get whiteGrayText6 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText6);

  /// key_windowBackgroundWhiteGrayText7
  Color get whiteGrayText7 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText7);

  /// key_windowBackgroundWhiteGrayText8
  Color get whiteGrayText8 =>
      _resolve(TelegramColorKey.windowBackgroundWhiteGrayText8);

  /// key_windowBackgroundWhiteBlackText
  Color get whiteBlackText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlackText);

  /// key_windowBackgroundWhiteHintText
  Color get whiteHintText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteHintText);

  /// key_windowBackgroundWhiteValueText
  Color get whiteValueText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteValueText);

  /// key_windowBackgroundWhiteLinkText
  Color get whiteLinkText =>
      _resolve(TelegramColorKey.windowBackgroundWhiteLinkText);

  /// key_windowBackgroundWhiteLinkSelection
  Color get whiteLinkSelection =>
      _resolve(TelegramColorKey.windowBackgroundWhiteLinkSelection);

  /// key_windowBackgroundWhiteBlueHeader
  Color get whiteBlueHeader =>
      _resolve(TelegramColorKey.windowBackgroundWhiteBlueHeader);

  /// key_windowBackgroundGray
  Color get gray => _resolve(TelegramColorKey.windowBackgroundGray);

  /// key_windowBackgroundGrayShadow
  Color get grayShadow => _resolve(TelegramColorKey.windowBackgroundGrayShadow);
}

/// dialog* — alert dialog surfaces, text and controls
class DialogColors {
  const DialogColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_dialogBackground
  Color get background => _resolve(TelegramColorKey.dialogBackground);

  /// key_dialogBackgroundGray
  Color get backgroundGray => _resolve(TelegramColorKey.dialogBackgroundGray);

  /// key_dialogTextBlack
  Color get textBlack => _resolve(TelegramColorKey.dialogTextBlack);

  /// key_dialogTextLink
  Color get textLink => _resolve(TelegramColorKey.dialogTextLink);

  /// key_dialogLinkSelection
  Color get linkSelection => _resolve(TelegramColorKey.dialogLinkSelection);

  /// key_dialogTextBlue
  Color get textBlue => _resolve(TelegramColorKey.dialogTextBlue);

  /// key_dialogTextBlue2
  Color get textBlue2 => _resolve(TelegramColorKey.dialogTextBlue2);

  /// key_dialogTextBlue4
  Color get textBlue4 => _resolve(TelegramColorKey.dialogTextBlue4);

  /// key_dialogTextGray
  Color get textGray => _resolve(TelegramColorKey.dialogTextGray);

  /// key_dialogTextGray2
  Color get textGray2 => _resolve(TelegramColorKey.dialogTextGray2);

  /// key_dialogTextGray3
  Color get textGray3 => _resolve(TelegramColorKey.dialogTextGray3);

  /// key_dialogTextGray4
  Color get textGray4 => _resolve(TelegramColorKey.dialogTextGray4);

  /// key_dialogTextHint
  Color get textHint => _resolve(TelegramColorKey.dialogTextHint);

  /// key_dialogInputField
  Color get inputField => _resolve(TelegramColorKey.dialogInputField);

  /// key_dialogInputFieldActivated
  Color get inputFieldActivated =>
      _resolve(TelegramColorKey.dialogInputFieldActivated);

  /// key_dialogCheckboxSquareBackground
  Color get checkboxSquareBackground =>
      _resolve(TelegramColorKey.dialogCheckboxSquareBackground);

  /// key_dialogCheckboxSquareCheck
  Color get checkboxSquareCheck =>
      _resolve(TelegramColorKey.dialogCheckboxSquareCheck);

  /// key_dialogCheckboxSquareUnchecked
  Color get checkboxSquareUnchecked =>
      _resolve(TelegramColorKey.dialogCheckboxSquareUnchecked);

  /// key_dialogCheckboxSquareDisabled
  Color get checkboxSquareDisabled =>
      _resolve(TelegramColorKey.dialogCheckboxSquareDisabled);

  /// key_dialogScrollGlow
  Color get scrollGlow => _resolve(TelegramColorKey.dialogScrollGlow);

  /// key_dialogRoundCheckBox
  Color get roundCheckBox => _resolve(TelegramColorKey.dialogRoundCheckBox);

  /// key_dialogRoundCheckBoxCheck
  Color get roundCheckBoxCheck =>
      _resolve(TelegramColorKey.dialogRoundCheckBoxCheck);

  /// key_dialogRadioBackground
  Color get radioBackground => _resolve(TelegramColorKey.dialogRadioBackground);

  /// key_dialogRadioBackgroundChecked
  Color get radioBackgroundChecked =>
      _resolve(TelegramColorKey.dialogRadioBackgroundChecked);

  /// key_dialogLineProgress
  Color get lineProgress => _resolve(TelegramColorKey.dialogLineProgress);

  /// key_dialogLineProgressBackground
  Color get lineProgressBackground =>
      _resolve(TelegramColorKey.dialogLineProgressBackground);

  /// key_dialogButton
  Color get button => _resolve(TelegramColorKey.dialogButton);

  /// key_dialogButtonSelector
  Color get buttonSelector => _resolve(TelegramColorKey.dialogButtonSelector);

  /// key_dialogIcon
  Color get icon => _resolve(TelegramColorKey.dialogIcon);

  /// key_dialogGrayLine
  Color get grayLine => _resolve(TelegramColorKey.dialogGrayLine);

  /// key_dialogTopBackground
  Color get topBackground => _resolve(TelegramColorKey.dialogTopBackground);

  /// key_dialog_inlineProgressBackground
  Color get inlineProgressBackground =>
      _resolve(TelegramColorKey.dialog_inlineProgressBackground);

  /// key_dialog_inlineProgress
  Color get inlineProgress => _resolve(TelegramColorKey.dialog_inlineProgress);

  /// key_dialogSearchBackground
  Color get searchBackground =>
      _resolve(TelegramColorKey.dialogSearchBackground);

  /// key_dialogSearchHint
  Color get searchHint => _resolve(TelegramColorKey.dialogSearchHint);

  /// key_dialogSearchIcon
  Color get searchIcon => _resolve(TelegramColorKey.dialogSearchIcon);

  /// key_dialogSearchText
  Color get searchText => _resolve(TelegramColorKey.dialogSearchText);

  /// key_dialogFloatingButton
  Color get floatingButton => _resolve(TelegramColorKey.dialogFloatingButton);

  /// key_dialogFloatingButtonPressed
  Color get floatingButtonPressed =>
      _resolve(TelegramColorKey.dialogFloatingButtonPressed);

  /// key_dialogFloatingIcon
  Color get floatingIcon => _resolve(TelegramColorKey.dialogFloatingIcon);

  /// key_dialogShadowLine
  Color get shadowLine => _resolve(TelegramColorKey.dialogShadowLine);

  /// key_dialogEmptyImage
  Color get emptyImage => _resolve(TelegramColorKey.dialogEmptyImage);

  /// key_dialogEmptyText
  Color get emptyText => _resolve(TelegramColorKey.dialogEmptyText);

  /// key_dialogSwipeRemove
  Color get swipeRemove => _resolve(TelegramColorKey.dialogSwipeRemove);

  /// key_dialogReactionMentionBackground
  Color get reactionMentionBackground =>
      _resolve(TelegramColorKey.dialogReactionMentionBackground);

  /// key_dialogCardShadow
  Color get cardShadow => _resolve(TelegramColorKey.dialogCardShadow);

  /// key_dialogGiftsBackground
  Color get giftsBackground => _resolve(TelegramColorKey.dialogGiftsBackground);

  /// key_dialogGiftsTabText
  Color get giftsTabText => _resolve(TelegramColorKey.dialogGiftsTabText);

  /// key_dialog_liveLocationProgress
  Color get liveLocationProgress =>
      _resolve(TelegramColorKey.dialog_liveLocationProgress);
}

/// actionBar* — action bar chrome, tabs, submenus
class ActionBarColors {
  const ActionBarColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_actionBarDefault
  Color get actionBarDefault => _resolve(TelegramColorKey.actionBarDefault);

  /// key_actionBarDefaultSelector
  Color get defaultSelector =>
      _resolve(TelegramColorKey.actionBarDefaultSelector);

  /// key_actionBarWhiteSelector
  Color get whiteSelector => _resolve(TelegramColorKey.actionBarWhiteSelector);

  /// key_actionBarDefaultIcon
  Color get defaultIcon => _resolve(TelegramColorKey.actionBarDefaultIcon);

  /// key_actionBarActionModeDefault
  Color get actionModeDefault =>
      _resolve(TelegramColorKey.actionBarActionModeDefault);

  /// key_actionBarActionModeDefaultTop
  Color get actionModeDefaultTop =>
      _resolve(TelegramColorKey.actionBarActionModeDefaultTop);

  /// key_actionBarActionModeDefaultIcon
  Color get actionModeDefaultIcon =>
      _resolve(TelegramColorKey.actionBarActionModeDefaultIcon);

  /// key_actionBarActionModeDefaultSelector
  Color get actionModeDefaultSelector =>
      _resolve(TelegramColorKey.actionBarActionModeDefaultSelector);

  /// key_actionBarDefaultTitle
  Color get defaultTitle => _resolve(TelegramColorKey.actionBarDefaultTitle);

  /// key_actionBarDefaultSubtitle
  Color get defaultSubtitle =>
      _resolve(TelegramColorKey.actionBarDefaultSubtitle);

  /// key_actionBarDefaultSearch
  Color get defaultSearch => _resolve(TelegramColorKey.actionBarDefaultSearch);

  /// key_actionBarDefaultSearchPlaceholder
  Color get defaultSearchPlaceholder =>
      _resolve(TelegramColorKey.actionBarDefaultSearchPlaceholder);

  /// key_actionBarDefaultSubmenuItem
  Color get defaultSubmenuItem =>
      _resolve(TelegramColorKey.actionBarDefaultSubmenuItem);

  /// key_actionBarDefaultSubmenuItemIcon
  Color get defaultSubmenuItemIcon =>
      _resolve(TelegramColorKey.actionBarDefaultSubmenuItemIcon);

  /// key_actionBarDefaultSubmenuBackground
  Color get defaultSubmenuBackground =>
      _resolve(TelegramColorKey.actionBarDefaultSubmenuBackground);

  /// key_actionBarDefaultSubmenuSeparator
  Color get defaultSubmenuSeparator =>
      _resolve(TelegramColorKey.actionBarDefaultSubmenuSeparator);

  /// key_actionBarTabActiveText
  Color get tabActiveText => _resolve(TelegramColorKey.actionBarTabActiveText);

  /// key_actionBarTabUnactiveText
  Color get tabUnactiveText =>
      _resolve(TelegramColorKey.actionBarTabUnactiveText);

  /// key_actionBarTabLine
  Color get tabLine => _resolve(TelegramColorKey.actionBarTabLine);

  /// key_actionBarTabSelector
  Color get tabSelector => _resolve(TelegramColorKey.actionBarTabSelector);

  /// key_actionBarDefaultArchived
  Color get defaultArchived =>
      _resolve(TelegramColorKey.actionBarDefaultArchived);

  /// key_actionBarDefaultArchivedSelector
  Color get defaultArchivedSelector =>
      _resolve(TelegramColorKey.actionBarDefaultArchivedSelector);

  /// key_actionBarDefaultArchivedIcon
  Color get defaultArchivedIcon =>
      _resolve(TelegramColorKey.actionBarDefaultArchivedIcon);

  /// key_actionBarDefaultArchivedTitle
  Color get defaultArchivedTitle =>
      _resolve(TelegramColorKey.actionBarDefaultArchivedTitle);

  /// key_actionBarDefaultArchivedSearch
  Color get defaultArchivedSearch =>
      _resolve(TelegramColorKey.actionBarDefaultArchivedSearch);

  /// key_actionBarDefaultArchivedSearchPlaceholder
  Color get defaultArchivedSearchPlaceholder =>
      _resolve(TelegramColorKey.actionBarDefaultArchivedSearchPlaceholder);

  /// key_actionBarBrowser
  Color get browser => _resolve(TelegramColorKey.actionBarBrowser);

  /// key_actionBarActionModeReaction
  Color get actionModeReaction =>
      _resolve(TelegramColorKey.actionBarActionModeReaction);

  /// key_actionBarActionModeReactionText
  Color get actionModeReactionText =>
      _resolve(TelegramColorKey.actionBarActionModeReactionText);

  /// key_actionBarActionModeReactionDot
  Color get actionModeReactionDot =>
      _resolve(TelegramColorKey.actionBarActionModeReactionDot);
}

/// chats_* — the dialogs (chat list) screen
class DialogsColors {
  const DialogsColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_chats_onlineCircle
  Color get onlineCircle => _resolve(TelegramColorKey.chats_onlineCircle);

  /// key_chats_unreadCounter
  Color get unreadCounter => _resolve(TelegramColorKey.chats_unreadCounter);

  /// key_chats_unreadCounterMuted
  Color get unreadCounterMuted =>
      _resolve(TelegramColorKey.chats_unreadCounterMuted);

  /// key_chats_unreadCounterText
  Color get unreadCounterText =>
      _resolve(TelegramColorKey.chats_unreadCounterText);

  /// key_chats_name
  Color get name => _resolve(TelegramColorKey.chats_name);

  /// key_chats_nameArchived
  Color get nameArchived => _resolve(TelegramColorKey.chats_nameArchived);

  /// key_chats_secretName
  Color get secretName => _resolve(TelegramColorKey.chats_secretName);

  /// key_chats_secretIcon
  Color get secretIcon => _resolve(TelegramColorKey.chats_secretIcon);

  /// key_chats_pinnedIcon
  Color get pinnedIcon => _resolve(TelegramColorKey.chats_pinnedIcon);

  /// key_chats_archiveBackground
  Color get archiveBackground =>
      _resolve(TelegramColorKey.chats_archiveBackground);

  /// key_chats_archivePinBackground
  Color get archivePinBackground =>
      _resolve(TelegramColorKey.chats_archivePinBackground);

  /// key_chats_archiveIcon
  Color get archiveIcon => _resolve(TelegramColorKey.chats_archiveIcon);

  /// key_chats_archiveText
  Color get archiveText => _resolve(TelegramColorKey.chats_archiveText);

  /// key_chats_message
  Color get message => _resolve(TelegramColorKey.chats_message);

  /// key_chats_messageArchived
  Color get messageArchived => _resolve(TelegramColorKey.chats_messageArchived);

  /// key_chats_message_threeLines
  Color get messageThreeLines =>
      _resolve(TelegramColorKey.chats_message_threeLines);

  /// key_chats_draft
  Color get draft => _resolve(TelegramColorKey.chats_draft);

  /// key_chats_nameMessage
  Color get nameMessage => _resolve(TelegramColorKey.chats_nameMessage);

  /// key_chats_nameMessageArchived
  Color get nameMessageArchived =>
      _resolve(TelegramColorKey.chats_nameMessageArchived);

  /// key_chats_nameMessage_threeLines
  Color get nameMessageThreeLines =>
      _resolve(TelegramColorKey.chats_nameMessage_threeLines);

  /// key_chats_nameMessageArchived_threeLines
  Color get nameMessageArchivedThreeLines =>
      _resolve(TelegramColorKey.chats_nameMessageArchived_threeLines);

  /// key_chats_attachMessage
  Color get attachMessage => _resolve(TelegramColorKey.chats_attachMessage);

  /// key_chats_actionMessage
  Color get actionMessage => _resolve(TelegramColorKey.chats_actionMessage);

  /// key_chats_date
  Color get date => _resolve(TelegramColorKey.chats_date);

  /// key_chats_date_bold
  Color get dateBold => _resolve(TelegramColorKey.chats_date_bold);

  /// key_chats_pinnedOverlay
  Color get pinnedOverlay => _resolve(TelegramColorKey.chats_pinnedOverlay);

  /// key_chats_tabletSelectedOverlay
  Color get tabletSelectedOverlay =>
      _resolve(TelegramColorKey.chats_tabletSelectedOverlay);

  /// key_chats_sentCheck
  Color get sentCheck => _resolve(TelegramColorKey.chats_sentCheck);

  /// key_chats_sentReadCheck
  Color get sentReadCheck => _resolve(TelegramColorKey.chats_sentReadCheck);

  /// key_chats_sentClock
  Color get sentClock => _resolve(TelegramColorKey.chats_sentClock);

  /// key_chats_sentError
  Color get sentError => _resolve(TelegramColorKey.chats_sentError);

  /// key_chats_sentErrorIcon
  Color get sentErrorIcon => _resolve(TelegramColorKey.chats_sentErrorIcon);

  /// key_chats_verifiedBackground
  Color get verifiedBackground =>
      _resolve(TelegramColorKey.chats_verifiedBackground);

  /// key_chats_verifiedCheck
  Color get verifiedCheck => _resolve(TelegramColorKey.chats_verifiedCheck);

  /// key_chats_muteIcon
  Color get muteIcon => _resolve(TelegramColorKey.chats_muteIcon);

  /// key_chats_mentionIcon
  Color get mentionIcon => _resolve(TelegramColorKey.chats_mentionIcon);

  /// key_chats_menuTopShadow
  Color get menuTopShadow => _resolve(TelegramColorKey.chats_menuTopShadow);

  /// key_chats_menuTopShadowCats
  Color get menuTopShadowCats =>
      _resolve(TelegramColorKey.chats_menuTopShadowCats);

  /// key_chats_menuBackground
  Color get menuBackground => _resolve(TelegramColorKey.chats_menuBackground);

  /// key_chats_menuItemText
  Color get menuItemText => _resolve(TelegramColorKey.chats_menuItemText);

  /// key_chats_menuItemCheck
  Color get menuItemCheck => _resolve(TelegramColorKey.chats_menuItemCheck);

  /// key_chats_menuItemIcon
  Color get menuItemIcon => _resolve(TelegramColorKey.chats_menuItemIcon);

  /// key_chats_menuName
  Color get menuName => _resolve(TelegramColorKey.chats_menuName);

  /// key_chats_menuPhone
  Color get menuPhone => _resolve(TelegramColorKey.chats_menuPhone);

  /// key_chats_menuPhoneCats
  Color get menuPhoneCats => _resolve(TelegramColorKey.chats_menuPhoneCats);

  /// key_chats_menuTopBackgroundCats
  Color get menuTopBackgroundCats =>
      _resolve(TelegramColorKey.chats_menuTopBackgroundCats);

  /// key_chats_menuTopBackground
  Color get menuTopBackground =>
      _resolve(TelegramColorKey.chats_menuTopBackground);

  /// key_chats_actionIcon
  Color get actionIcon => _resolve(TelegramColorKey.chats_actionIcon);

  /// key_chats_actionBackground
  Color get actionBackground =>
      _resolve(TelegramColorKey.chats_actionBackground);

  /// key_chats_actionPressedBackground
  Color get actionPressedBackground =>
      _resolve(TelegramColorKey.chats_actionPressedBackground);

  /// key_chats_archivePullDownBackground
  Color get archivePullDownBackground =>
      _resolve(TelegramColorKey.chats_archivePullDownBackground);

  /// key_chats_archivePullDownBackgroundActive
  Color get archivePullDownBackgroundActive =>
      _resolve(TelegramColorKey.chats_archivePullDownBackgroundActive);

  /// key_chats_tabUnreadActiveBackground
  Color get tabUnreadActiveBackground =>
      _resolve(TelegramColorKey.chats_tabUnreadActiveBackground);

  /// key_chats_tabUnreadUnactiveBackground
  Color get tabUnreadUnactiveBackground =>
      _resolve(TelegramColorKey.chats_tabUnreadUnactiveBackground);
}

/// chat_* — chat screen: bubbles, service messages, input panel
class ChatColors {
  const ChatColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_chat_attachCheckBoxCheck
  Color get attachCheckBoxCheck =>
      _resolve(TelegramColorKey.chat_attachCheckBoxCheck);

  /// key_chat_attachCheckBoxBackground
  Color get attachCheckBoxBackground =>
      _resolve(TelegramColorKey.chat_attachCheckBoxBackground);

  /// key_chat_attachPhotoBackground
  Color get attachPhotoBackground =>
      _resolve(TelegramColorKey.chat_attachPhotoBackground);

  /// key_chat_attachActiveTab
  Color get attachActiveTab => _resolve(TelegramColorKey.chat_attachActiveTab);

  /// key_chat_attachUnactiveTab
  Color get attachUnactiveTab =>
      _resolve(TelegramColorKey.chat_attachUnactiveTab);

  /// key_chat_attachPermissionImage
  Color get attachPermissionImage =>
      _resolve(TelegramColorKey.chat_attachPermissionImage);

  /// key_chat_attachPermissionMark
  Color get attachPermissionMark =>
      _resolve(TelegramColorKey.chat_attachPermissionMark);

  /// key_chat_attachPermissionText
  Color get attachPermissionText =>
      _resolve(TelegramColorKey.chat_attachPermissionText);

  /// key_chat_attachEmptyImage
  Color get attachEmptyImage =>
      _resolve(TelegramColorKey.chat_attachEmptyImage);

  /// key_chat_inPollCorrectAnswer
  Color get inPollCorrectAnswer =>
      _resolve(TelegramColorKey.chat_inPollCorrectAnswer);

  /// key_chat_outPollCorrectAnswer
  Color get outPollCorrectAnswer =>
      _resolve(TelegramColorKey.chat_outPollCorrectAnswer);

  /// key_chat_inPollWrongAnswer
  Color get inPollWrongAnswer =>
      _resolve(TelegramColorKey.chat_inPollWrongAnswer);

  /// key_chat_outPollWrongAnswer
  Color get outPollWrongAnswer =>
      _resolve(TelegramColorKey.chat_outPollWrongAnswer);

  /// key_chat_attachIcon
  Color get attachIcon => _resolve(TelegramColorKey.chat_attachIcon);

  /// key_chat_attachGalleryBackground
  Color get attachGalleryBackground =>
      _resolve(TelegramColorKey.chat_attachGalleryBackground);

  /// key_chat_attachAudioBackground
  Color get attachAudioBackground =>
      _resolve(TelegramColorKey.chat_attachAudioBackground);

  /// key_chat_attachContactBackground
  Color get attachContactBackground =>
      _resolve(TelegramColorKey.chat_attachContactBackground);

  /// key_chat_attachContactText
  Color get attachContactText =>
      _resolve(TelegramColorKey.chat_attachContactText);

  /// key_chat_attachLocationBackground
  Color get attachLocationBackground =>
      _resolve(TelegramColorKey.chat_attachLocationBackground);

  /// key_chat_attachPollBackground
  Color get attachPollBackground =>
      _resolve(TelegramColorKey.chat_attachPollBackground);

  /// key_chat_status
  Color get status => _resolve(TelegramColorKey.chat_status);

  /// key_chat_inGreenCall
  Color get inGreenCall => _resolve(TelegramColorKey.chat_inGreenCall);

  /// key_chat_inBubble
  Color get inBubble => _resolve(TelegramColorKey.chat_inBubble);

  /// key_chat_inBubbleSelectedOverlay
  Color get inBubbleSelectedOverlay =>
      _resolve(TelegramColorKey.chat_inBubbleSelectedOverlay);

  /// key_chat_inBubbleShadow
  Color get inBubbleShadow => _resolve(TelegramColorKey.chat_inBubbleShadow);

  /// key_chat_outBubble
  Color get outBubble => _resolve(TelegramColorKey.chat_outBubble);

  /// key_chat_outBubbleSelected
  Color get outBubbleSelected =>
      _resolve(TelegramColorKey.chat_outBubbleSelected);

  /// key_chat_outBubbleShadow
  Color get outBubbleShadow => _resolve(TelegramColorKey.chat_outBubbleShadow);

  /// key_chat_outBubbleGradient1
  Color get outBubbleGradient1 =>
      _resolve(TelegramColorKey.chat_outBubbleGradient1);

  /// key_chat_outBubbleGradient2
  Color get outBubbleGradient2 =>
      _resolve(TelegramColorKey.chat_outBubbleGradient2);

  /// key_chat_outBubbleGradient3
  Color get outBubbleGradient3 =>
      _resolve(TelegramColorKey.chat_outBubbleGradient3);

  /// key_chat_outGreenCall
  Color get outGreenCall => _resolve(TelegramColorKey.chat_outGreenCall);

  /// key_chat_outSentCheck
  Color get outSentCheck => _resolve(TelegramColorKey.chat_outSentCheck);

  /// key_chat_outSentCheckSelected
  Color get outSentCheckSelected =>
      _resolve(TelegramColorKey.chat_outSentCheckSelected);

  /// key_chat_outSentCheckRead
  Color get outSentCheckRead =>
      _resolve(TelegramColorKey.chat_outSentCheckRead);

  /// key_chat_outSentCheckReadSelected
  Color get outSentCheckReadSelected =>
      _resolve(TelegramColorKey.chat_outSentCheckReadSelected);

  /// key_chat_outSentClock
  Color get outSentClock => _resolve(TelegramColorKey.chat_outSentClock);

  /// key_chat_outSentClockSelected
  Color get outSentClockSelected =>
      _resolve(TelegramColorKey.chat_outSentClockSelected);

  /// key_chat_outMediaIcon
  Color get outMediaIcon => _resolve(TelegramColorKey.chat_outMediaIcon);

  /// key_chat_outMediaIconSelected
  Color get outMediaIconSelected =>
      _resolve(TelegramColorKey.chat_outMediaIconSelected);

  /// key_chat_outViews
  Color get outViews => _resolve(TelegramColorKey.chat_outViews);

  /// key_chat_outViewsSelected
  Color get outViewsSelected =>
      _resolve(TelegramColorKey.chat_outViewsSelected);

  /// key_chat_outMenu
  Color get outMenu => _resolve(TelegramColorKey.chat_outMenu);

  /// key_chat_outMenuSelected
  Color get outMenuSelected => _resolve(TelegramColorKey.chat_outMenuSelected);

  /// key_chat_outInstant
  Color get outInstant => _resolve(TelegramColorKey.chat_outInstant);

  /// key_chat_outInstantSelected
  Color get outInstantSelected =>
      _resolve(TelegramColorKey.chat_outInstantSelected);

  /// key_chat_outPreviewInstantText
  Color get outPreviewInstantText =>
      _resolve(TelegramColorKey.chat_outPreviewInstantText);

  /// key_chat_outForwardedNameText
  Color get outForwardedNameText =>
      _resolve(TelegramColorKey.chat_outForwardedNameText);

  /// key_chat_outViaBotNameText
  Color get outViaBotNameText =>
      _resolve(TelegramColorKey.chat_outViaBotNameText);

  /// key_chat_outReplyLine
  Color get outReplyLine => _resolve(TelegramColorKey.chat_outReplyLine);

  /// key_chat_outReplyLine2
  Color get outReplyLine2 => _resolve(TelegramColorKey.chat_outReplyLine2);

  /// key_chat_outReplyNameText
  Color get outReplyNameText =>
      _resolve(TelegramColorKey.chat_outReplyNameText);

  /// key_chat_outReplyMessageText
  Color get outReplyMessageText =>
      _resolve(TelegramColorKey.chat_outReplyMessageText);

  /// key_chat_outReplyMediaMessageText
  Color get outReplyMediaMessageText =>
      _resolve(TelegramColorKey.chat_outReplyMediaMessageText);

  /// key_chat_outReplyMediaMessageSelectedText
  Color get outReplyMediaMessageSelectedText =>
      _resolve(TelegramColorKey.chat_outReplyMediaMessageSelectedText);

  /// key_chat_outPreviewLine
  Color get outPreviewLine => _resolve(TelegramColorKey.chat_outPreviewLine);

  /// key_chat_outSiteNameText
  Color get outSiteNameText => _resolve(TelegramColorKey.chat_outSiteNameText);

  /// key_chat_outContactNameText
  Color get outContactNameText =>
      _resolve(TelegramColorKey.chat_outContactNameText);

  /// key_chat_outContactPhoneText
  Color get outContactPhoneText =>
      _resolve(TelegramColorKey.chat_outContactPhoneText);

  /// key_chat_outContactPhoneSelectedText
  Color get outContactPhoneSelectedText =>
      _resolve(TelegramColorKey.chat_outContactPhoneSelectedText);

  /// key_chat_outAudioPerformerText
  Color get outAudioPerformerText =>
      _resolve(TelegramColorKey.chat_outAudioPerformerText);

  /// key_chat_outAudioPerformerSelectedText
  Color get outAudioPerformerSelectedText =>
      _resolve(TelegramColorKey.chat_outAudioPerformerSelectedText);

  /// key_chat_outTimeSelectedText
  Color get outTimeSelectedText =>
      _resolve(TelegramColorKey.chat_outTimeSelectedText);

  /// key_chat_outAdminText
  Color get outAdminText => _resolve(TelegramColorKey.chat_outAdminText);

  /// key_chat_outAdminSelectedText
  Color get outAdminSelectedText =>
      _resolve(TelegramColorKey.chat_outAdminSelectedText);

  /// key_chat_outAudioProgress
  Color get outAudioProgress =>
      _resolve(TelegramColorKey.chat_outAudioProgress);

  /// key_chat_outAudioSelectedProgress
  Color get outAudioSelectedProgress =>
      _resolve(TelegramColorKey.chat_outAudioSelectedProgress);

  /// key_chat_outTimeText
  Color get outTimeText => _resolve(TelegramColorKey.chat_outTimeText);

  /// key_chat_outAudioTitleText
  Color get outAudioTitleText =>
      _resolve(TelegramColorKey.chat_outAudioTitleText);

  /// key_chat_outAudioDurationText
  Color get outAudioDurationText =>
      _resolve(TelegramColorKey.chat_outAudioDurationText);

  /// key_chat_outAudioDurationSelectedText
  Color get outAudioDurationSelectedText =>
      _resolve(TelegramColorKey.chat_outAudioDurationSelectedText);

  /// key_chat_outAudioSeekbar
  Color get outAudioSeekbar => _resolve(TelegramColorKey.chat_outAudioSeekbar);

  /// key_chat_outAudioCacheSeekbar
  Color get outAudioCacheSeekbar =>
      _resolve(TelegramColorKey.chat_outAudioCacheSeekbar);

  /// key_chat_outAudioSeekbarSelected
  Color get outAudioSeekbarSelected =>
      _resolve(TelegramColorKey.chat_outAudioSeekbarSelected);

  /// key_chat_outAudioSeekbarFill
  Color get outAudioSeekbarFill =>
      _resolve(TelegramColorKey.chat_outAudioSeekbarFill);

  /// key_chat_outVoiceSeekbar
  Color get outVoiceSeekbar => _resolve(TelegramColorKey.chat_outVoiceSeekbar);

  /// key_chat_outVoiceSeekbarSelected
  Color get outVoiceSeekbarSelected =>
      _resolve(TelegramColorKey.chat_outVoiceSeekbarSelected);

  /// key_chat_outVoiceSeekbarFill
  Color get outVoiceSeekbarFill =>
      _resolve(TelegramColorKey.chat_outVoiceSeekbarFill);

  /// key_chat_outFileProgress
  Color get outFileProgress => _resolve(TelegramColorKey.chat_outFileProgress);

  /// key_chat_outFileProgressSelected
  Color get outFileProgressSelected =>
      _resolve(TelegramColorKey.chat_outFileProgressSelected);

  /// key_chat_outFileNameText
  Color get outFileNameText => _resolve(TelegramColorKey.chat_outFileNameText);

  /// key_chat_outFileInfoText
  Color get outFileInfoText => _resolve(TelegramColorKey.chat_outFileInfoText);

  /// key_chat_outFileInfoSelectedText
  Color get outFileInfoSelectedText =>
      _resolve(TelegramColorKey.chat_outFileInfoSelectedText);

  /// key_chat_outFileBackground
  Color get outFileBackground =>
      _resolve(TelegramColorKey.chat_outFileBackground);

  /// key_chat_outFileBackgroundSelected
  Color get outFileBackgroundSelected =>
      _resolve(TelegramColorKey.chat_outFileBackgroundSelected);

  /// key_chat_outVenueInfoText
  Color get outVenueInfoText =>
      _resolve(TelegramColorKey.chat_outVenueInfoText);

  /// key_chat_outVenueInfoSelectedText
  Color get outVenueInfoSelectedText =>
      _resolve(TelegramColorKey.chat_outVenueInfoSelectedText);

  /// key_chat_outLinkSelectBackground
  Color get outLinkSelectBackground =>
      _resolve(TelegramColorKey.chat_outLinkSelectBackground);

  /// key_chat_outLoader
  Color get outLoader => _resolve(TelegramColorKey.chat_outLoader);

  /// key_chat_outLoaderSelected
  Color get outLoaderSelected =>
      _resolve(TelegramColorKey.chat_outLoaderSelected);

  /// key_chat_outLocationIcon
  Color get outLocationIcon => _resolve(TelegramColorKey.chat_outLocationIcon);

  /// key_chat_outContactBackground
  Color get outContactBackground =>
      _resolve(TelegramColorKey.chat_outContactBackground);

  /// key_chat_outContactIcon
  Color get outContactIcon => _resolve(TelegramColorKey.chat_outContactIcon);

  /// key_chat_outReactionButtonBackground
  Color get outReactionButtonBackground =>
      _resolve(TelegramColorKey.chat_outReactionButtonBackground);

  /// key_chat_outTextSelectionHighlight
  Color get outTextSelectionHighlight =>
      _resolve(TelegramColorKey.chat_outTextSelectionHighlight);

  /// key_chat_outTextSelectionCursor
  Color get outTextSelectionCursor =>
      _resolve(TelegramColorKey.chat_outTextSelectionCursor);

  /// key_chat_outBubbleLocationPlaceholder
  Color get outBubbleLocationPlaceholder =>
      _resolve(TelegramColorKey.chat_outBubbleLocationPlaceholder);

  /// key_chat_outBubbleSelectedOverlay
  Color get outBubbleSelectedOverlay =>
      _resolve(TelegramColorKey.chat_outBubbleSelectedOverlay);

  /// key_chat_outPsaNameText
  Color get outPsaNameText => _resolve(TelegramColorKey.chat_outPsaNameText);

  /// key_chat_outBubbleGradientAnimated
  Color get outBubbleGradientAnimated =>
      _resolve(TelegramColorKey.chat_outBubbleGradientAnimated);

  /// key_chat_outBubbleGradientSelectedOverlay
  Color get outBubbleGradientSelectedOverlay =>
      _resolve(TelegramColorKey.chat_outBubbleGradientSelectedOverlay);

  /// key_chat_inBubbleSelected
  Color get inBubbleSelected =>
      _resolve(TelegramColorKey.chat_inBubbleSelected);

  /// key_chat_messageTextIn
  Color get messageTextIn => _resolve(TelegramColorKey.chat_messageTextIn);

  /// key_chat_messageTextOut
  Color get messageTextOut => _resolve(TelegramColorKey.chat_messageTextOut);

  /// key_chat_messageLinkIn
  Color get messageLinkIn => _resolve(TelegramColorKey.chat_messageLinkIn);

  /// key_chat_messageLinkOut
  Color get messageLinkOut => _resolve(TelegramColorKey.chat_messageLinkOut);

  /// key_chat_serviceText
  Color get serviceText => _resolve(TelegramColorKey.chat_serviceText);

  /// key_chat_serviceLink
  Color get serviceLink => _resolve(TelegramColorKey.chat_serviceLink);

  /// key_chat_serviceIcon
  Color get serviceIcon => _resolve(TelegramColorKey.chat_serviceIcon);

  /// key_chat_serviceBackground
  Color get serviceBackground =>
      _resolve(TelegramColorKey.chat_serviceBackground);

  /// key_chat_serviceBackgroundSelected
  Color get serviceBackgroundSelected =>
      _resolve(TelegramColorKey.chat_serviceBackgroundSelected);

  /// key_chat_serviceBackgroundSelector
  Color get serviceBackgroundSelector =>
      _resolve(TelegramColorKey.chat_serviceBackgroundSelector);

  /// key_chat_muteIcon
  Color get muteIcon => _resolve(TelegramColorKey.chat_muteIcon);

  /// key_chat_lockIcon
  Color get lockIcon => _resolve(TelegramColorKey.chat_lockIcon);

  /// key_chat_inSentClock
  Color get inSentClock => _resolve(TelegramColorKey.chat_inSentClock);

  /// key_chat_inSentClockSelected
  Color get inSentClockSelected =>
      _resolve(TelegramColorKey.chat_inSentClockSelected);

  /// key_chat_mediaSentCheck
  Color get mediaSentCheck => _resolve(TelegramColorKey.chat_mediaSentCheck);

  /// key_chat_mediaSentClock
  Color get mediaSentClock => _resolve(TelegramColorKey.chat_mediaSentClock);

  /// key_chat_inMediaIcon
  Color get inMediaIcon => _resolve(TelegramColorKey.chat_inMediaIcon);

  /// key_chat_inMediaIconSelected
  Color get inMediaIconSelected =>
      _resolve(TelegramColorKey.chat_inMediaIconSelected);

  /// key_chat_mediaTimeBackground
  Color get mediaTimeBackground =>
      _resolve(TelegramColorKey.chat_mediaTimeBackground);

  /// key_chat_inViews
  Color get inViews => _resolve(TelegramColorKey.chat_inViews);

  /// key_chat_inViewsSelected
  Color get inViewsSelected => _resolve(TelegramColorKey.chat_inViewsSelected);

  /// key_chat_mediaViews
  Color get mediaViews => _resolve(TelegramColorKey.chat_mediaViews);

  /// key_chat_inMenu
  Color get inMenu => _resolve(TelegramColorKey.chat_inMenu);

  /// key_chat_inMenuSelected
  Color get inMenuSelected => _resolve(TelegramColorKey.chat_inMenuSelected);

  /// key_chat_mediaMenu
  Color get mediaMenu => _resolve(TelegramColorKey.chat_mediaMenu);

  /// key_chat_inInstant
  Color get inInstant => _resolve(TelegramColorKey.chat_inInstant);

  /// key_chat_inInstantSelected
  Color get inInstantSelected =>
      _resolve(TelegramColorKey.chat_inInstantSelected);

  /// key_chat_sentError
  Color get sentError => _resolve(TelegramColorKey.chat_sentError);

  /// key_chat_sentErrorIcon
  Color get sentErrorIcon => _resolve(TelegramColorKey.chat_sentErrorIcon);

  /// key_chat_selectedBackground
  Color get selectedBackground =>
      _resolve(TelegramColorKey.chat_selectedBackground);

  /// key_chat_previewDurationText
  Color get previewDurationText =>
      _resolve(TelegramColorKey.chat_previewDurationText);

  /// key_chat_previewGameText
  Color get previewGameText => _resolve(TelegramColorKey.chat_previewGameText);

  /// key_chat_inPreviewInstantText
  Color get inPreviewInstantText =>
      _resolve(TelegramColorKey.chat_inPreviewInstantText);

  /// key_chat_secretTimeText
  Color get secretTimeText => _resolve(TelegramColorKey.chat_secretTimeText);

  /// key_chat_stickerNameText
  Color get stickerNameText => _resolve(TelegramColorKey.chat_stickerNameText);

  /// key_chat_botButtonText
  Color get botButtonText => _resolve(TelegramColorKey.chat_botButtonText);

  /// key_chat_inForwardedNameText
  Color get inForwardedNameText =>
      _resolve(TelegramColorKey.chat_inForwardedNameText);

  /// key_chat_inPsaNameText
  Color get inPsaNameText => _resolve(TelegramColorKey.chat_inPsaNameText);

  /// key_chat_inViaBotNameText
  Color get inViaBotNameText =>
      _resolve(TelegramColorKey.chat_inViaBotNameText);

  /// key_chat_stickerViaBotNameText
  Color get stickerViaBotNameText =>
      _resolve(TelegramColorKey.chat_stickerViaBotNameText);

  /// key_chat_inQuote
  Color get inQuote => _resolve(TelegramColorKey.chat_inQuote);

  /// key_chat_outQuote
  Color get outQuote => _resolve(TelegramColorKey.chat_outQuote);

  /// key_chat_inReplyLine
  Color get inReplyLine => _resolve(TelegramColorKey.chat_inReplyLine);

  /// key_chat_stickerReplyLine
  Color get stickerReplyLine =>
      _resolve(TelegramColorKey.chat_stickerReplyLine);

  /// key_chat_inReplyNameText
  Color get inReplyNameText => _resolve(TelegramColorKey.chat_inReplyNameText);

  /// key_chat_stickerReplyNameText
  Color get stickerReplyNameText =>
      _resolve(TelegramColorKey.chat_stickerReplyNameText);

  /// key_chat_inReplyMessageText
  Color get inReplyMessageText =>
      _resolve(TelegramColorKey.chat_inReplyMessageText);

  /// key_chat_inReplyMediaMessageText
  Color get inReplyMediaMessageText =>
      _resolve(TelegramColorKey.chat_inReplyMediaMessageText);

  /// key_chat_inReplyMediaMessageSelectedText
  Color get inReplyMediaMessageSelectedText =>
      _resolve(TelegramColorKey.chat_inReplyMediaMessageSelectedText);

  /// key_chat_stickerReplyMessageText
  Color get stickerReplyMessageText =>
      _resolve(TelegramColorKey.chat_stickerReplyMessageText);

  /// key_chat_inPreviewLine
  Color get inPreviewLine => _resolve(TelegramColorKey.chat_inPreviewLine);

  /// key_chat_inSiteNameText
  Color get inSiteNameText => _resolve(TelegramColorKey.chat_inSiteNameText);

  /// key_chat_inContactNameText
  Color get inContactNameText =>
      _resolve(TelegramColorKey.chat_inContactNameText);

  /// key_chat_inContactPhoneText
  Color get inContactPhoneText =>
      _resolve(TelegramColorKey.chat_inContactPhoneText);

  /// key_chat_inContactPhoneSelectedText
  Color get inContactPhoneSelectedText =>
      _resolve(TelegramColorKey.chat_inContactPhoneSelectedText);

  /// key_chat_mediaProgress
  Color get mediaProgress => _resolve(TelegramColorKey.chat_mediaProgress);

  /// key_chat_inAudioProgress
  Color get inAudioProgress => _resolve(TelegramColorKey.chat_inAudioProgress);

  /// key_chat_inAudioSelectedProgress
  Color get inAudioSelectedProgress =>
      _resolve(TelegramColorKey.chat_inAudioSelectedProgress);

  /// key_chat_mediaTimeText
  Color get mediaTimeText => _resolve(TelegramColorKey.chat_mediaTimeText);

  /// key_chat_inAdminText
  Color get inAdminText => _resolve(TelegramColorKey.chat_inAdminText);

  /// key_chat_inAdminSelectedText
  Color get inAdminSelectedText =>
      _resolve(TelegramColorKey.chat_inAdminSelectedText);

  /// key_chat_inTimeText
  Color get inTimeText => _resolve(TelegramColorKey.chat_inTimeText);

  /// key_chat_inTimeSelectedText
  Color get inTimeSelectedText =>
      _resolve(TelegramColorKey.chat_inTimeSelectedText);

  /// key_chat_inAudioPerformerText
  Color get inAudioPerformerText =>
      _resolve(TelegramColorKey.chat_inAudioPerformerText);

  /// key_chat_inAudioPerformerSelectedText
  Color get inAudioPerformerSelectedText =>
      _resolve(TelegramColorKey.chat_inAudioPerformerSelectedText);

  /// key_chat_inAudioTitleText
  Color get inAudioTitleText =>
      _resolve(TelegramColorKey.chat_inAudioTitleText);

  /// key_chat_inAudioDurationText
  Color get inAudioDurationText =>
      _resolve(TelegramColorKey.chat_inAudioDurationText);

  /// key_chat_inAudioDurationSelectedText
  Color get inAudioDurationSelectedText =>
      _resolve(TelegramColorKey.chat_inAudioDurationSelectedText);

  /// key_chat_inAudioSeekbar
  Color get inAudioSeekbar => _resolve(TelegramColorKey.chat_inAudioSeekbar);

  /// key_chat_inAudioCacheSeekbar
  Color get inAudioCacheSeekbar =>
      _resolve(TelegramColorKey.chat_inAudioCacheSeekbar);

  /// key_chat_inAudioSeekbarSelected
  Color get inAudioSeekbarSelected =>
      _resolve(TelegramColorKey.chat_inAudioSeekbarSelected);

  /// key_chat_inAudioSeekbarFill
  Color get inAudioSeekbarFill =>
      _resolve(TelegramColorKey.chat_inAudioSeekbarFill);

  /// key_chat_inVoiceSeekbar
  Color get inVoiceSeekbar => _resolve(TelegramColorKey.chat_inVoiceSeekbar);

  /// key_chat_inVoiceSeekbarSelected
  Color get inVoiceSeekbarSelected =>
      _resolve(TelegramColorKey.chat_inVoiceSeekbarSelected);

  /// key_chat_inVoiceSeekbarFill
  Color get inVoiceSeekbarFill =>
      _resolve(TelegramColorKey.chat_inVoiceSeekbarFill);

  /// key_chat_inFileProgress
  Color get inFileProgress => _resolve(TelegramColorKey.chat_inFileProgress);

  /// key_chat_inFileProgressSelected
  Color get inFileProgressSelected =>
      _resolve(TelegramColorKey.chat_inFileProgressSelected);

  /// key_chat_inFileNameText
  Color get inFileNameText => _resolve(TelegramColorKey.chat_inFileNameText);

  /// key_chat_inFileInfoText
  Color get inFileInfoText => _resolve(TelegramColorKey.chat_inFileInfoText);

  /// key_chat_inFileInfoSelectedText
  Color get inFileInfoSelectedText =>
      _resolve(TelegramColorKey.chat_inFileInfoSelectedText);

  /// key_chat_inFileBackground
  Color get inFileBackground =>
      _resolve(TelegramColorKey.chat_inFileBackground);

  /// key_chat_inFileBackgroundSelected
  Color get inFileBackgroundSelected =>
      _resolve(TelegramColorKey.chat_inFileBackgroundSelected);

  /// key_chat_inVenueInfoText
  Color get inVenueInfoText => _resolve(TelegramColorKey.chat_inVenueInfoText);

  /// key_chat_inVenueInfoSelectedText
  Color get inVenueInfoSelectedText =>
      _resolve(TelegramColorKey.chat_inVenueInfoSelectedText);

  /// key_chat_mediaInfoText
  Color get mediaInfoText => _resolve(TelegramColorKey.chat_mediaInfoText);

  /// key_chat_linkSelectBackground
  Color get linkSelectBackground =>
      _resolve(TelegramColorKey.chat_linkSelectBackground);

  /// key_chat_textSelectBackground
  Color get textSelectBackground =>
      _resolve(TelegramColorKey.chat_textSelectBackground);

  /// key_chat_wallpaper
  Color get wallpaper => _resolve(TelegramColorKey.chat_wallpaper);

  /// key_chat_wallpaper_gradient_to1
  Color get wallpaperGradientTo1 =>
      _resolve(TelegramColorKey.chat_wallpaper_gradient_to1);

  /// key_chat_wallpaper_gradient_to2
  Color get wallpaperGradientTo2 =>
      _resolve(TelegramColorKey.chat_wallpaper_gradient_to2);

  /// key_chat_wallpaper_gradient_to3
  Color get wallpaperGradientTo3 =>
      _resolve(TelegramColorKey.chat_wallpaper_gradient_to3);

  /// key_chat_wallpaper_gradient_rotation
  Color get wallpaperGradientRotation =>
      _resolve(TelegramColorKey.chat_wallpaper_gradient_rotation);

  /// key_chat_messagePanelBackground
  Color get messagePanelBackground =>
      _resolve(TelegramColorKey.chat_messagePanelBackground);

  /// key_chat_messagePanelShadow
  Color get messagePanelShadow =>
      _resolve(TelegramColorKey.chat_messagePanelShadow);

  /// key_chat_messagePanelText
  Color get messagePanelText =>
      _resolve(TelegramColorKey.chat_messagePanelText);

  /// key_chat_messagePanelHint
  Color get messagePanelHint =>
      _resolve(TelegramColorKey.chat_messagePanelHint);

  /// key_chat_messagePanelCursor
  Color get messagePanelCursor =>
      _resolve(TelegramColorKey.chat_messagePanelCursor);

  /// key_chat_messagePanelIcons
  Color get messagePanelIcons =>
      _resolve(TelegramColorKey.chat_messagePanelIcons);

  /// key_chat_messagePanelSend
  Color get messagePanelSend =>
      _resolve(TelegramColorKey.chat_messagePanelSend);

  /// key_chat_messagePanelVoiceLock
  Color get messagePanelVoiceLock =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceLock);

  /// key_chat_messagePanelVoiceLockBackground
  Color get messagePanelVoiceLockBackground =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceLockBackground);

  /// key_chat_messagePanelVoiceLockShadow
  Color get messagePanelVoiceLockShadow =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceLockShadow);

  /// key_chat_topPanelBackground
  Color get topPanelBackground =>
      _resolve(TelegramColorKey.chat_topPanelBackground);

  /// key_chat_topPanelClose
  Color get topPanelClose => _resolve(TelegramColorKey.chat_topPanelClose);

  /// key_chat_topPanelLine
  Color get topPanelLine => _resolve(TelegramColorKey.chat_topPanelLine);

  /// key_chat_topPanelTitle
  Color get topPanelTitle => _resolve(TelegramColorKey.chat_topPanelTitle);

  /// key_chat_topPanelMessage
  Color get topPanelMessage => _resolve(TelegramColorKey.chat_topPanelMessage);

  /// key_chat_addContact
  Color get addContact => _resolve(TelegramColorKey.chat_addContact);

  /// key_chat_inLoader
  Color get inLoader => _resolve(TelegramColorKey.chat_inLoader);

  /// key_chat_inLoaderSelected
  Color get inLoaderSelected =>
      _resolve(TelegramColorKey.chat_inLoaderSelected);

  /// key_chat_inLoaderPhoto
  Color get inLoaderPhoto => _resolve(TelegramColorKey.chat_inLoaderPhoto);

  /// key_chat_mediaLoaderPhoto
  Color get mediaLoaderPhoto =>
      _resolve(TelegramColorKey.chat_mediaLoaderPhoto);

  /// key_chat_mediaLoaderPhotoSelected
  Color get mediaLoaderPhotoSelected =>
      _resolve(TelegramColorKey.chat_mediaLoaderPhotoSelected);

  /// key_chat_mediaLoaderPhotoIcon
  Color get mediaLoaderPhotoIcon =>
      _resolve(TelegramColorKey.chat_mediaLoaderPhotoIcon);

  /// key_chat_mediaLoaderPhotoIconSelected
  Color get mediaLoaderPhotoIconSelected =>
      _resolve(TelegramColorKey.chat_mediaLoaderPhotoIconSelected);

  /// key_chat_inLocationBackground
  Color get inLocationBackground =>
      _resolve(TelegramColorKey.chat_inLocationBackground);

  /// key_chat_inLocationIcon
  Color get inLocationIcon => _resolve(TelegramColorKey.chat_inLocationIcon);

  /// key_chat_inContactBackground
  Color get inContactBackground =>
      _resolve(TelegramColorKey.chat_inContactBackground);

  /// key_chat_inContactIcon
  Color get inContactIcon => _resolve(TelegramColorKey.chat_inContactIcon);

  /// key_chat_replyPanelIcons
  Color get replyPanelIcons => _resolve(TelegramColorKey.chat_replyPanelIcons);

  /// key_chat_replyPanelClose
  Color get replyPanelClose => _resolve(TelegramColorKey.chat_replyPanelClose);

  /// key_chat_replyPanelName
  Color get replyPanelName => _resolve(TelegramColorKey.chat_replyPanelName);

  /// key_chat_replyPanelLine
  Color get replyPanelLine => _resolve(TelegramColorKey.chat_replyPanelLine);

  /// key_chat_searchPanelIcons
  Color get searchPanelIcons =>
      _resolve(TelegramColorKey.chat_searchPanelIcons);

  /// key_chat_searchPanelText
  Color get searchPanelText => _resolve(TelegramColorKey.chat_searchPanelText);

  /// key_chat_secretChatStatusText
  Color get secretChatStatusText =>
      _resolve(TelegramColorKey.chat_secretChatStatusText);

  /// key_chat_fieldOverlayText
  Color get fieldOverlayText =>
      _resolve(TelegramColorKey.chat_fieldOverlayText);

  /// key_chat_stickersHintPanel
  Color get stickersHintPanel =>
      _resolve(TelegramColorKey.chat_stickersHintPanel);

  /// key_chat_botSwitchToInlineText
  Color get botSwitchToInlineText =>
      _resolve(TelegramColorKey.chat_botSwitchToInlineText);

  /// key_chat_unreadMessagesStartArrowIcon
  Color get unreadMessagesStartArrowIcon =>
      _resolve(TelegramColorKey.chat_unreadMessagesStartArrowIcon);

  /// key_chat_unreadMessagesStartText
  Color get unreadMessagesStartText =>
      _resolve(TelegramColorKey.chat_unreadMessagesStartText);

  /// key_chat_unreadMessagesStartBackground
  Color get unreadMessagesStartBackground =>
      _resolve(TelegramColorKey.chat_unreadMessagesStartBackground);

  /// key_chat_inlineResultIcon
  Color get inlineResultIcon =>
      _resolve(TelegramColorKey.chat_inlineResultIcon);

  /// key_chat_emojiPanelBackground
  Color get emojiPanelBackground =>
      _resolve(TelegramColorKey.chat_emojiPanelBackground);

  /// key_chat_emojiSearchBackground
  Color get emojiSearchBackground =>
      _resolve(TelegramColorKey.chat_emojiSearchBackground);

  /// key_chat_emojiSearchIcon
  Color get emojiSearchIcon => _resolve(TelegramColorKey.chat_emojiSearchIcon);

  /// key_chat_emojiPanelShadowLine
  Color get emojiPanelShadowLine =>
      _resolve(TelegramColorKey.chat_emojiPanelShadowLine);

  /// key_chat_emojiPanelEmptyText
  Color get emojiPanelEmptyText =>
      _resolve(TelegramColorKey.chat_emojiPanelEmptyText);

  /// key_chat_emojiPanelIcon
  Color get emojiPanelIcon => _resolve(TelegramColorKey.chat_emojiPanelIcon);

  /// key_chat_emojiBottomPanelIcon
  Color get emojiBottomPanelIcon =>
      _resolve(TelegramColorKey.chat_emojiBottomPanelIcon);

  /// key_chat_emojiPanelIconSelected
  Color get emojiPanelIconSelected =>
      _resolve(TelegramColorKey.chat_emojiPanelIconSelected);

  /// key_chat_emojiPanelStickerPackSelector
  Color get emojiPanelStickerPackSelector =>
      _resolve(TelegramColorKey.chat_emojiPanelStickerPackSelector);

  /// key_chat_emojiPanelStickerPackSelectorLine
  Color get emojiPanelStickerPackSelectorLine =>
      _resolve(TelegramColorKey.chat_emojiPanelStickerPackSelectorLine);

  /// key_chat_emojiPanelBackspace
  Color get emojiPanelBackspace =>
      _resolve(TelegramColorKey.chat_emojiPanelBackspace);

  /// key_chat_emojiPanelTrendingTitle
  Color get emojiPanelTrendingTitle =>
      _resolve(TelegramColorKey.chat_emojiPanelTrendingTitle);

  /// key_chat_emojiPanelStickerSetName
  Color get emojiPanelStickerSetName =>
      _resolve(TelegramColorKey.chat_emojiPanelStickerSetName);

  /// key_chat_emojiPanelStickerSetNameHighlight
  Color get emojiPanelStickerSetNameHighlight =>
      _resolve(TelegramColorKey.chat_emojiPanelStickerSetNameHighlight);

  /// key_chat_emojiPanelStickerSetNameIcon
  Color get emojiPanelStickerSetNameIcon =>
      _resolve(TelegramColorKey.chat_emojiPanelStickerSetNameIcon);

  /// key_chat_emojiPanelTrendingDescription
  Color get emojiPanelTrendingDescription =>
      _resolve(TelegramColorKey.chat_emojiPanelTrendingDescription);

  /// key_chat_botKeyboardButtonText
  Color get botKeyboardButtonText =>
      _resolve(TelegramColorKey.chat_botKeyboardButtonText);

  /// key_chat_botKeyboardButtonBackground
  Color get botKeyboardButtonBackground =>
      _resolve(TelegramColorKey.chat_botKeyboardButtonBackground);

  /// key_chat_botKeyboardButtonBackgroundPressed
  Color get botKeyboardButtonBackgroundPressed =>
      _resolve(TelegramColorKey.chat_botKeyboardButtonBackgroundPressed);

  /// key_chat_emojiPanelNewTrending
  Color get emojiPanelNewTrending =>
      _resolve(TelegramColorKey.chat_emojiPanelNewTrending);

  /// key_chat_messagePanelVoicePressed
  Color get messagePanelVoicePressed =>
      _resolve(TelegramColorKey.chat_messagePanelVoicePressed);

  /// key_chat_messagePanelVoiceBackground
  Color get messagePanelVoiceBackground =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceBackground);

  /// key_chat_messagePanelVoiceDelete
  Color get messagePanelVoiceDelete =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceDelete);

  /// key_chat_messagePanelVoiceDuration
  Color get messagePanelVoiceDuration =>
      _resolve(TelegramColorKey.chat_messagePanelVoiceDuration);

  /// key_chat_recordedVoicePlayPause
  Color get recordedVoicePlayPause =>
      _resolve(TelegramColorKey.chat_recordedVoicePlayPause);

  /// key_chat_recordedVoiceProgress
  Color get recordedVoiceProgress =>
      _resolve(TelegramColorKey.chat_recordedVoiceProgress);

  /// key_chat_recordedVoiceProgressInner
  Color get recordedVoiceProgressInner =>
      _resolve(TelegramColorKey.chat_recordedVoiceProgressInner);

  /// key_chat_recordedVoiceDot
  Color get recordedVoiceDot =>
      _resolve(TelegramColorKey.chat_recordedVoiceDot);

  /// key_chat_recordedVoiceBackground
  Color get recordedVoiceBackground =>
      _resolve(TelegramColorKey.chat_recordedVoiceBackground);

  /// key_chat_recordedVoiceDarkerBackground
  Color get recordedVoiceDarkerBackground =>
      _resolve(TelegramColorKey.chat_recordedVoiceDarkerBackground);

  /// key_chat_recordVoiceCancel
  Color get recordVoiceCancel =>
      _resolve(TelegramColorKey.chat_recordVoiceCancel);

  /// key_chat_recordTime
  Color get recordTime => _resolve(TelegramColorKey.chat_recordTime);

  /// key_chat_messagePanelCancelInlineBot
  Color get messagePanelCancelInlineBot =>
      _resolve(TelegramColorKey.chat_messagePanelCancelInlineBot);

  /// key_chat_gifSaveHintText
  Color get gifSaveHintText => _resolve(TelegramColorKey.chat_gifSaveHintText);

  /// key_chat_gifSaveHintBackground
  Color get gifSaveHintBackground =>
      _resolve(TelegramColorKey.chat_gifSaveHintBackground);

  /// key_chat_goDownButton
  Color get goDownButton => _resolve(TelegramColorKey.chat_goDownButton);

  /// key_chat_goDownButtonCounter
  Color get goDownButtonCounter =>
      _resolve(TelegramColorKey.chat_goDownButtonCounter);

  /// key_chat_goDownButtonCounterBackground
  Color get goDownButtonCounterBackground =>
      _resolve(TelegramColorKey.chat_goDownButtonCounterBackground);

  /// key_chat_inTextSelectionHighlight
  Color get inTextSelectionHighlight =>
      _resolve(TelegramColorKey.chat_inTextSelectionHighlight);

  /// key_chat_TextSelectionCursor
  Color get textSelectionCursor =>
      _resolve(TelegramColorKey.chat_TextSelectionCursor);

  /// key_chat_inBubbleLocationPlaceholder
  Color get inBubbleLocationPlaceholder =>
      _resolve(TelegramColorKey.chat_inBubbleLocationPlaceholder);

  /// key_chat_BlurAlpha
  Color get blurAlpha => _resolve(TelegramColorKey.chat_BlurAlpha);

  /// key_chat_BlurAlphaSlow
  Color get blurAlphaSlow => _resolve(TelegramColorKey.chat_BlurAlphaSlow);

  /// key_chat_editMediaButton
  Color get editMediaButton => _resolve(TelegramColorKey.chat_editMediaButton);

  /// key_chat_tagAdmin
  Color get tagAdmin => _resolve(TelegramColorKey.chat_tagAdmin);

  /// key_chat_tagCreator
  Color get tagCreator => _resolve(TelegramColorKey.chat_tagCreator);

  /// key_chat_inReactionButtonBackground
  Color get inReactionButtonBackground =>
      _resolve(TelegramColorKey.chat_inReactionButtonBackground);

  /// key_chat_outReactionButtonText
  Color get outReactionButtonText =>
      _resolve(TelegramColorKey.chat_outReactionButtonText);

  /// key_chat_inReactionButtonText
  Color get inReactionButtonText =>
      _resolve(TelegramColorKey.chat_inReactionButtonText);

  /// key_chat_inReactionButtonTextSelected
  Color get inReactionButtonTextSelected =>
      _resolve(TelegramColorKey.chat_inReactionButtonTextSelected);

  /// key_chat_outReactionButtonTextSelected
  Color get outReactionButtonTextSelected =>
      _resolve(TelegramColorKey.chat_outReactionButtonTextSelected);

  /// key_chat_reactionServiceButtonBackgroundSelected
  Color get reactionServiceButtonBackgroundSelected =>
      _resolve(TelegramColorKey.chat_reactionServiceButtonBackgroundSelected);

  /// key_chat_reactionServiceButtonTextSelected
  Color get reactionServiceButtonTextSelected =>
      _resolve(TelegramColorKey.chat_reactionServiceButtonTextSelected);

  /// key_chat_inCodeBackground
  Color get inCodeBackground =>
      _resolve(TelegramColorKey.chat_inCodeBackground);

  /// key_chat_outCodeBackground
  Color get outCodeBackground =>
      _resolve(TelegramColorKey.chat_outCodeBackground);
}

/// avatar_* — avatar fills and action-bar tints
class AvatarColors {
  const AvatarColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_avatar_text
  Color get text => _resolve(TelegramColorKey.avatar_text);

  /// key_avatar_backgroundSaved
  Color get backgroundSaved =>
      _resolve(TelegramColorKey.avatar_backgroundSaved);

  /// key_avatar_background2Saved
  Color get background2Saved =>
      _resolve(TelegramColorKey.avatar_background2Saved);

  /// key_avatar_backgroundArchived
  Color get backgroundArchived =>
      _resolve(TelegramColorKey.avatar_backgroundArchived);

  /// key_avatar_backgroundArchivedHidden
  Color get backgroundArchivedHidden =>
      _resolve(TelegramColorKey.avatar_backgroundArchivedHidden);

  /// key_avatar_backgroundRed
  Color get backgroundRed => _resolve(TelegramColorKey.avatar_backgroundRed);

  /// key_avatar_backgroundOrange
  Color get backgroundOrange =>
      _resolve(TelegramColorKey.avatar_backgroundOrange);

  /// key_avatar_backgroundViolet
  Color get backgroundViolet =>
      _resolve(TelegramColorKey.avatar_backgroundViolet);

  /// key_avatar_backgroundGreen
  Color get backgroundGreen =>
      _resolve(TelegramColorKey.avatar_backgroundGreen);

  /// key_avatar_backgroundCyan
  Color get backgroundCyan => _resolve(TelegramColorKey.avatar_backgroundCyan);

  /// key_avatar_backgroundBlue
  Color get backgroundBlue => _resolve(TelegramColorKey.avatar_backgroundBlue);

  /// key_avatar_backgroundPink
  Color get backgroundPink => _resolve(TelegramColorKey.avatar_backgroundPink);

  /// key_avatar_background2Red
  Color get background2Red => _resolve(TelegramColorKey.avatar_background2Red);

  /// key_avatar_background2Orange
  Color get background2Orange =>
      _resolve(TelegramColorKey.avatar_background2Orange);

  /// key_avatar_background2Violet
  Color get background2Violet =>
      _resolve(TelegramColorKey.avatar_background2Violet);

  /// key_avatar_background2Green
  Color get background2Green =>
      _resolve(TelegramColorKey.avatar_background2Green);

  /// key_avatar_background2Cyan
  Color get background2Cyan =>
      _resolve(TelegramColorKey.avatar_background2Cyan);

  /// key_avatar_background2Blue
  Color get background2Blue =>
      _resolve(TelegramColorKey.avatar_background2Blue);

  /// key_avatar_background2Pink
  Color get background2Pink =>
      _resolve(TelegramColorKey.avatar_background2Pink);

  /// key_avatar_backgroundGray
  Color get backgroundGray => _resolve(TelegramColorKey.avatar_backgroundGray);

  /// key_avatar_backgroundInProfileBlue
  Color get backgroundInProfileBlue =>
      _resolve(TelegramColorKey.avatar_backgroundInProfileBlue);

  /// key_avatar_backgroundActionBarBlue
  Color get backgroundActionBarBlue =>
      _resolve(TelegramColorKey.avatar_backgroundActionBarBlue);

  /// key_avatar_actionBarSelectorBlue
  Color get actionBarSelectorBlue =>
      _resolve(TelegramColorKey.avatar_actionBarSelectorBlue);

  /// key_avatar_actionBarIconBlue
  Color get actionBarIconBlue =>
      _resolve(TelegramColorKey.avatar_actionBarIconBlue);

  /// key_avatar_subtitleInProfileBlue
  Color get subtitleInProfileBlue =>
      _resolve(TelegramColorKey.avatar_subtitleInProfileBlue);

  /// key_avatar_nameInMessageRed
  Color get nameInMessageRed =>
      _resolve(TelegramColorKey.avatar_nameInMessageRed);

  /// key_avatar_nameInMessageOrange
  Color get nameInMessageOrange =>
      _resolve(TelegramColorKey.avatar_nameInMessageOrange);

  /// key_avatar_nameInMessageViolet
  Color get nameInMessageViolet =>
      _resolve(TelegramColorKey.avatar_nameInMessageViolet);

  /// key_avatar_nameInMessageGreen
  Color get nameInMessageGreen =>
      _resolve(TelegramColorKey.avatar_nameInMessageGreen);

  /// key_avatar_nameInMessageCyan
  Color get nameInMessageCyan =>
      _resolve(TelegramColorKey.avatar_nameInMessageCyan);

  /// key_avatar_nameInMessageBlue
  Color get nameInMessageBlue =>
      _resolve(TelegramColorKey.avatar_nameInMessageBlue);

  /// key_avatar_nameInMessagePink
  Color get nameInMessagePink =>
      _resolve(TelegramColorKey.avatar_nameInMessagePink);
}

/// voipgroup_* — group calls UI
class VoipGroupColors {
  const VoipGroupColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_voipgroup_listSelector
  Color get listSelector => _resolve(TelegramColorKey.voipgroup_listSelector);

  /// key_voipgroup_inviteMembersBackground
  Color get inviteMembersBackground =>
      _resolve(TelegramColorKey.voipgroup_inviteMembersBackground);

  /// key_voipgroup_actionBar
  Color get actionBar => _resolve(TelegramColorKey.voipgroup_actionBar);

  /// key_voipgroup_actionBarItems
  Color get actionBarItems =>
      _resolve(TelegramColorKey.voipgroup_actionBarItems);

  /// key_voipgroup_actionBarItemsSelector
  Color get actionBarItemsSelector =>
      _resolve(TelegramColorKey.voipgroup_actionBarItemsSelector);

  /// key_voipgroup_actionBarUnscrolled
  Color get actionBarUnscrolled =>
      _resolve(TelegramColorKey.voipgroup_actionBarUnscrolled);

  /// key_voipgroup_listViewBackgroundUnscrolled
  Color get listViewBackgroundUnscrolled =>
      _resolve(TelegramColorKey.voipgroup_listViewBackgroundUnscrolled);

  /// key_voipgroup_lastSeenTextUnscrolled
  Color get lastSeenTextUnscrolled =>
      _resolve(TelegramColorKey.voipgroup_lastSeenTextUnscrolled);

  /// key_voipgroup_mutedIconUnscrolled
  Color get mutedIconUnscrolled =>
      _resolve(TelegramColorKey.voipgroup_mutedIconUnscrolled);

  /// key_voipgroup_nameText
  Color get nameText => _resolve(TelegramColorKey.voipgroup_nameText);

  /// key_voipgroup_lastSeenText
  Color get lastSeenText => _resolve(TelegramColorKey.voipgroup_lastSeenText);

  /// key_voipgroup_listeningText
  Color get listeningText => _resolve(TelegramColorKey.voipgroup_listeningText);

  /// key_voipgroup_speakingText
  Color get speakingText => _resolve(TelegramColorKey.voipgroup_speakingText);

  /// key_voipgroup_mutedIcon
  Color get mutedIcon => _resolve(TelegramColorKey.voipgroup_mutedIcon);

  /// key_voipgroup_mutedByAdminIcon
  Color get mutedByAdminIcon =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminIcon);

  /// key_voipgroup_listViewBackground
  Color get listViewBackground =>
      _resolve(TelegramColorKey.voipgroup_listViewBackground);

  /// key_voipgroup_dialogBackground
  Color get dialogBackground =>
      _resolve(TelegramColorKey.voipgroup_dialogBackground);

  /// key_voipgroup_leaveCallMenu
  Color get leaveCallMenu => _resolve(TelegramColorKey.voipgroup_leaveCallMenu);

  /// key_voipgroup_checkMenu
  Color get checkMenu => _resolve(TelegramColorKey.voipgroup_checkMenu);

  /// key_voipgroup_soundButton
  Color get soundButton => _resolve(TelegramColorKey.voipgroup_soundButton);

  /// key_voipgroup_soundButtonActive
  Color get soundButtonActive =>
      _resolve(TelegramColorKey.voipgroup_soundButtonActive);

  /// key_voipgroup_soundButtonActiveScrolled
  Color get soundButtonActiveScrolled =>
      _resolve(TelegramColorKey.voipgroup_soundButtonActiveScrolled);

  /// key_voipgroup_soundButton2
  Color get soundButton2 => _resolve(TelegramColorKey.voipgroup_soundButton2);

  /// key_voipgroup_soundButtonActive2
  Color get soundButtonActive2 =>
      _resolve(TelegramColorKey.voipgroup_soundButtonActive2);

  /// key_voipgroup_soundButtonActive2Scrolled
  Color get soundButtonActive2Scrolled =>
      _resolve(TelegramColorKey.voipgroup_soundButtonActive2Scrolled);

  /// key_voipgroup_leaveButton
  Color get leaveButton => _resolve(TelegramColorKey.voipgroup_leaveButton);

  /// key_voipgroup_leaveButtonScrolled
  Color get leaveButtonScrolled =>
      _resolve(TelegramColorKey.voipgroup_leaveButtonScrolled);

  /// key_voipgroup_muteButton
  Color get muteButton => _resolve(TelegramColorKey.voipgroup_muteButton);

  /// key_voipgroup_muteButton2
  Color get muteButton2 => _resolve(TelegramColorKey.voipgroup_muteButton2);

  /// key_voipgroup_muteButton3
  Color get muteButton3 => _resolve(TelegramColorKey.voipgroup_muteButton3);

  /// key_voipgroup_unmuteButton
  Color get unmuteButton => _resolve(TelegramColorKey.voipgroup_unmuteButton);

  /// key_voipgroup_unmuteButton2
  Color get unmuteButton2 => _resolve(TelegramColorKey.voipgroup_unmuteButton2);

  /// key_voipgroup_disabledButton
  Color get disabledButton =>
      _resolve(TelegramColorKey.voipgroup_disabledButton);

  /// key_voipgroup_rtmpButton
  Color get rtmpButton => _resolve(TelegramColorKey.voipgroup_rtmpButton);

  /// key_voipgroup_disabledButtonActive
  Color get disabledButtonActive =>
      _resolve(TelegramColorKey.voipgroup_disabledButtonActive);

  /// key_voipgroup_disabledButtonActiveScrolled
  Color get disabledButtonActiveScrolled =>
      _resolve(TelegramColorKey.voipgroup_disabledButtonActiveScrolled);

  /// key_voipgroup_connectingProgress
  Color get connectingProgress =>
      _resolve(TelegramColorKey.voipgroup_connectingProgress);

  /// key_voipgroup_scrollUp
  Color get scrollUp => _resolve(TelegramColorKey.voipgroup_scrollUp);

  /// key_voipgroup_searchPlaceholder
  Color get searchPlaceholder =>
      _resolve(TelegramColorKey.voipgroup_searchPlaceholder);

  /// key_voipgroup_searchBackground
  Color get searchBackground =>
      _resolve(TelegramColorKey.voipgroup_searchBackground);

  /// key_voipgroup_searchText
  Color get searchText => _resolve(TelegramColorKey.voipgroup_searchText);

  /// key_voipgroup_overlayGreen1
  Color get overlayGreen1 => _resolve(TelegramColorKey.voipgroup_overlayGreen1);

  /// key_voipgroup_overlayGreen2
  Color get overlayGreen2 => _resolve(TelegramColorKey.voipgroup_overlayGreen2);

  /// key_voipgroup_overlayBlue1
  Color get overlayBlue1 => _resolve(TelegramColorKey.voipgroup_overlayBlue1);

  /// key_voipgroup_overlayBlue2
  Color get overlayBlue2 => _resolve(TelegramColorKey.voipgroup_overlayBlue2);

  /// key_voipgroup_topPanelGreen1
  Color get topPanelGreen1 =>
      _resolve(TelegramColorKey.voipgroup_topPanelGreen1);

  /// key_voipgroup_topPanelGreen2
  Color get topPanelGreen2 =>
      _resolve(TelegramColorKey.voipgroup_topPanelGreen2);

  /// key_voipgroup_topPanelBlue1
  Color get topPanelBlue1 => _resolve(TelegramColorKey.voipgroup_topPanelBlue1);

  /// key_voipgroup_topPanelBlue2
  Color get topPanelBlue2 => _resolve(TelegramColorKey.voipgroup_topPanelBlue2);

  /// key_voipgroup_topPanelGray
  Color get topPanelGray => _resolve(TelegramColorKey.voipgroup_topPanelGray);

  /// key_voipgroup_overlayAlertGradientMuted
  Color get overlayAlertGradientMuted =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertGradientMuted);

  /// key_voipgroup_overlayAlertGradientMuted2
  Color get overlayAlertGradientMuted2 =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertGradientMuted2);

  /// key_voipgroup_overlayAlertGradientUnmuted
  Color get overlayAlertGradientUnmuted =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertGradientUnmuted);

  /// key_voipgroup_overlayAlertGradientUnmuted2
  Color get overlayAlertGradientUnmuted2 =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertGradientUnmuted2);

  /// key_voipgroup_overlayAlertMutedByAdmin
  Color get overlayAlertMutedByAdmin =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertMutedByAdmin);

  /// key_voipgroup_overlayAlertMutedByAdmin2
  Color get overlayAlertMutedByAdmin2 =>
      _resolve(TelegramColorKey.voipgroup_overlayAlertMutedByAdmin2);

  /// key_voipgroup_mutedByAdminGradient
  Color get mutedByAdminGradient =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminGradient);

  /// key_voipgroup_mutedByAdminGradient2
  Color get mutedByAdminGradient2 =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminGradient2);

  /// key_voipgroup_mutedByAdminGradient3
  Color get mutedByAdminGradient3 =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminGradient3);

  /// key_voipgroup_mutedByAdminMuteButton
  Color get mutedByAdminMuteButton =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminMuteButton);

  /// key_voipgroup_mutedByAdminMuteButtonDisabled
  Color get mutedByAdminMuteButtonDisabled =>
      _resolve(TelegramColorKey.voipgroup_mutedByAdminMuteButtonDisabled);

  /// key_voipgroup_windowBackgroundWhiteInputField
  Color get windowBackgroundWhiteInputField =>
      _resolve(TelegramColorKey.voipgroup_windowBackgroundWhiteInputField);

  /// key_voipgroup_windowBackgroundWhiteInputFieldActivated
  Color get windowBackgroundWhiteInputFieldActivated => _resolve(
    TelegramColorKey.voipgroup_windowBackgroundWhiteInputFieldActivated,
  );
}

/// profile_* — profile screen
class ProfileColors {
  const ProfileColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_profile_creatorIcon
  Color get creatorIcon => _resolve(TelegramColorKey.profile_creatorIcon);

  /// key_profile_title
  Color get title => _resolve(TelegramColorKey.profile_title);

  /// key_profile_actionIcon
  Color get actionIcon => _resolve(TelegramColorKey.profile_actionIcon);

  /// key_profile_actionBackground
  Color get actionBackground =>
      _resolve(TelegramColorKey.profile_actionBackground);

  /// key_profile_actionPressedBackground
  Color get actionPressedBackground =>
      _resolve(TelegramColorKey.profile_actionPressedBackground);

  /// key_profile_verifiedBackground
  Color get verifiedBackground =>
      _resolve(TelegramColorKey.profile_verifiedBackground);

  /// key_profile_verifiedCheck
  Color get verifiedCheck => _resolve(TelegramColorKey.profile_verifiedCheck);

  /// key_profile_status
  Color get status => _resolve(TelegramColorKey.profile_status);

  /// key_profile_tabText
  Color get tabText => _resolve(TelegramColorKey.profile_tabText);

  /// key_profile_tabSelectedText
  Color get tabSelectedText =>
      _resolve(TelegramColorKey.profile_tabSelectedText);

  /// key_profile_tabSelectedLine
  Color get tabSelectedLine =>
      _resolve(TelegramColorKey.profile_tabSelectedLine);

  /// key_profile_tabSelector
  Color get tabSelector => _resolve(TelegramColorKey.profile_tabSelector);
}

/// stories_* — stories viewer and circles
class StoriesColors {
  const StoriesColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_stories_circle1
  Color get circle1 => _resolve(TelegramColorKey.stories_circle1);

  /// key_stories_circle2
  Color get circle2 => _resolve(TelegramColorKey.stories_circle2);

  /// key_stories_circle_dialog1
  Color get circleDialog1 => _resolve(TelegramColorKey.stories_circle_dialog1);

  /// key_stories_circle_dialog2
  Color get circleDialog2 => _resolve(TelegramColorKey.stories_circle_dialog2);

  /// key_stories_circle_closeFriends1
  Color get circleCloseFriends1 =>
      _resolve(TelegramColorKey.stories_circle_closeFriends1);

  /// key_stories_circle_closeFriends2
  Color get circleCloseFriends2 =>
      _resolve(TelegramColorKey.stories_circle_closeFriends2);

  /// key_stories_circle_live1
  Color get circleLive1 => _resolve(TelegramColorKey.stories_circle_live1);

  /// key_stories_circle_live2
  Color get circleLive2 => _resolve(TelegramColorKey.stories_circle_live2);
}

/// premium* — premium promo gradients and buttons
class PremiumColors {
  const PremiumColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_premiumGradient0
  Color get gradient0 => _resolve(TelegramColorKey.premiumGradient0);

  /// key_premiumGradient1
  Color get gradient1 => _resolve(TelegramColorKey.premiumGradient1);

  /// key_premiumGradient2
  Color get gradient2 => _resolve(TelegramColorKey.premiumGradient2);

  /// key_premiumGradient3
  Color get gradient3 => _resolve(TelegramColorKey.premiumGradient3);

  /// key_premiumGradient4
  Color get gradient4 => _resolve(TelegramColorKey.premiumGradient4);

  /// key_premiumGradientBackground1
  Color get gradientBackground1 =>
      _resolve(TelegramColorKey.premiumGradientBackground1);

  /// key_premiumGradientBackground2
  Color get gradientBackground2 =>
      _resolve(TelegramColorKey.premiumGradientBackground2);

  /// key_premiumGradientBackground3
  Color get gradientBackground3 =>
      _resolve(TelegramColorKey.premiumGradientBackground3);

  /// key_premiumGradientBackground4
  Color get gradientBackground4 =>
      _resolve(TelegramColorKey.premiumGradientBackground4);

  /// key_premiumGradientBackgroundOverlay
  Color get gradientBackgroundOverlay =>
      _resolve(TelegramColorKey.premiumGradientBackgroundOverlay);

  /// key_premiumStartSmallStarsColor
  Color get startSmallStarsColor =>
      _resolve(TelegramColorKey.premiumStartSmallStarsColor);

  /// key_premiumStarGradient1
  Color get starGradient1 => _resolve(TelegramColorKey.premiumStarGradient1);

  /// key_premiumStarGradient2
  Color get starGradient2 => _resolve(TelegramColorKey.premiumStarGradient2);

  /// key_premiumCoinGradient1
  Color get coinGradient1 => _resolve(TelegramColorKey.premiumCoinGradient1);

  /// key_premiumCoinGradient2
  Color get coinGradient2 => _resolve(TelegramColorKey.premiumCoinGradient2);

  /// key_premiumStartSmallStarsColor2
  Color get startSmallStarsColor2 =>
      _resolve(TelegramColorKey.premiumStartSmallStarsColor2);

  /// key_premiumGradientBottomSheet1
  Color get gradientBottomSheet1 =>
      _resolve(TelegramColorKey.premiumGradientBottomSheet1);

  /// key_premiumGradientBottomSheet2
  Color get gradientBottomSheet2 =>
      _resolve(TelegramColorKey.premiumGradientBottomSheet2);

  /// key_premiumGradientBottomSheet3
  Color get gradientBottomSheet3 =>
      _resolve(TelegramColorKey.premiumGradientBottomSheet3);
}

/// statisticChart* — statistics charts
class StatisticColors {
  const StatisticColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_statisticChartSignature
  Color get chartSignature =>
      _resolve(TelegramColorKey.statisticChartSignature);

  /// key_statisticChartSignatureAlpha
  Color get chartSignatureAlpha =>
      _resolve(TelegramColorKey.statisticChartSignatureAlpha);

  /// key_statisticChartHintLine
  Color get chartHintLine => _resolve(TelegramColorKey.statisticChartHintLine);

  /// key_statisticChartActiveLine
  Color get chartActiveLine =>
      _resolve(TelegramColorKey.statisticChartActiveLine);

  /// key_statisticChartInactivePickerChart
  Color get chartInactivePickerChart =>
      _resolve(TelegramColorKey.statisticChartInactivePickerChart);

  /// key_statisticChartActivePickerChart
  Color get chartActivePickerChart =>
      _resolve(TelegramColorKey.statisticChartActivePickerChart);

  /// key_statisticChartRipple
  Color get chartRipple => _resolve(TelegramColorKey.statisticChartRipple);

  /// key_statisticChartBackZoomColor
  Color get chartBackZoomColor =>
      _resolve(TelegramColorKey.statisticChartBackZoomColor);

  /// key_statisticChartChevronColor
  Color get chartChevronColor =>
      _resolve(TelegramColorKey.statisticChartChevronColor);

  /// key_statisticChartLine_blue
  Color get chartLineBlue => _resolve(TelegramColorKey.statisticChartLine_blue);

  /// key_statisticChartLine_green
  Color get chartLineGreen =>
      _resolve(TelegramColorKey.statisticChartLine_green);

  /// key_statisticChartLine_red
  Color get chartLineRed => _resolve(TelegramColorKey.statisticChartLine_red);

  /// key_statisticChartLine_golden
  Color get chartLineGolden =>
      _resolve(TelegramColorKey.statisticChartLine_golden);

  /// key_statisticChartLine_lightblue
  Color get chartLineLightblue =>
      _resolve(TelegramColorKey.statisticChartLine_lightblue);

  /// key_statisticChartLine_lightgreen
  Color get chartLineLightgreen =>
      _resolve(TelegramColorKey.statisticChartLine_lightgreen);

  /// key_statisticChartLine_orange
  Color get chartLineOrange =>
      _resolve(TelegramColorKey.statisticChartLine_orange);

  /// key_statisticChartLine_indigo
  Color get chartLineIndigo =>
      _resolve(TelegramColorKey.statisticChartLine_indigo);

  /// key_statisticChartLine_purple
  Color get chartLinePurple =>
      _resolve(TelegramColorKey.statisticChartLine_purple);

  /// key_statisticChartLine_cyan
  Color get chartLineCyan => _resolve(TelegramColorKey.statisticChartLine_cyan);

  /// key_statisticChartLineEmpty
  Color get chartLineEmpty =>
      _resolve(TelegramColorKey.statisticChartLineEmpty);
}

/// location_* — location sharing UI
class LocationColors {
  const LocationColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_location_sendLocationBackground
  Color get sendLocationBackground =>
      _resolve(TelegramColorKey.location_sendLocationBackground);

  /// key_location_sendLocationIcon
  Color get sendLocationIcon =>
      _resolve(TelegramColorKey.location_sendLocationIcon);

  /// key_location_sendLocationText
  Color get sendLocationText =>
      _resolve(TelegramColorKey.location_sendLocationText);

  /// key_location_sendLiveLocationBackground
  Color get sendLiveLocationBackground =>
      _resolve(TelegramColorKey.location_sendLiveLocationBackground);

  /// key_location_sendLiveLocationIcon
  Color get sendLiveLocationIcon =>
      _resolve(TelegramColorKey.location_sendLiveLocationIcon);

  /// key_location_sendLiveLocationText
  Color get sendLiveLocationText =>
      _resolve(TelegramColorKey.location_sendLiveLocationText);

  /// key_location_liveLocationProgress
  Color get liveLocationProgress =>
      _resolve(TelegramColorKey.location_liveLocationProgress);

  /// key_location_placeLocationBackground
  Color get placeLocationBackground =>
      _resolve(TelegramColorKey.location_placeLocationBackground);

  /// key_location_actionIcon
  Color get actionIcon => _resolve(TelegramColorKey.location_actionIcon);

  /// key_location_actionActiveIcon
  Color get actionActiveIcon =>
      _resolve(TelegramColorKey.location_actionActiveIcon);

  /// key_location_actionBackground
  Color get actionBackground =>
      _resolve(TelegramColorKey.location_actionBackground);

  /// key_location_actionPressedBackground
  Color get actionPressedBackground =>
      _resolve(TelegramColorKey.location_actionPressedBackground);
}

/// player_* — full-screen audio player
class PlayerColors {
  const PlayerColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_player_actionBarSelector
  Color get actionBarSelector =>
      _resolve(TelegramColorKey.player_actionBarSelector);

  /// key_player_actionBarTitle
  Color get actionBarTitle => _resolve(TelegramColorKey.player_actionBarTitle);

  /// key_player_actionBarSubtitle
  Color get actionBarSubtitle =>
      _resolve(TelegramColorKey.player_actionBarSubtitle);

  /// key_player_actionBarItems
  Color get actionBarItems => _resolve(TelegramColorKey.player_actionBarItems);

  /// key_player_background
  Color get background => _resolve(TelegramColorKey.player_background);

  /// key_player_time
  Color get time => _resolve(TelegramColorKey.player_time);

  /// key_player_progressBackground
  Color get progressBackground =>
      _resolve(TelegramColorKey.player_progressBackground);

  /// key_player_progressCachedBackground
  Color get progressCachedBackground =>
      _resolve(TelegramColorKey.player_progressCachedBackground);

  /// key_player_progress
  Color get progress => _resolve(TelegramColorKey.player_progress);

  /// key_player_button
  Color get button => _resolve(TelegramColorKey.player_button);

  /// key_player_buttonActive
  Color get buttonActive => _resolve(TelegramColorKey.player_buttonActive);
}

/// inappPlayer* — the in-app mini player bar
class InAppPlayerColors {
  const InAppPlayerColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_inappPlayerPerformer
  Color get performer => _resolve(TelegramColorKey.inappPlayerPerformer);

  /// key_inappPlayerTitle
  Color get title => _resolve(TelegramColorKey.inappPlayerTitle);

  /// key_inappPlayerBackground
  Color get background => _resolve(TelegramColorKey.inappPlayerBackground);

  /// key_inappPlayerPlayPause
  Color get playPause => _resolve(TelegramColorKey.inappPlayerPlayPause);

  /// key_inappPlayerClose
  Color get close => _resolve(TelegramColorKey.inappPlayerClose);
}

/// switch* — switch tracks and thumbs
class SwitchColors {
  const SwitchColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_switchTrack
  Color get track => _resolve(TelegramColorKey.switchTrack);

  /// key_switchTrackChecked
  Color get trackChecked => _resolve(TelegramColorKey.switchTrackChecked);

  /// key_switchTrackBlue
  Color get trackBlue => _resolve(TelegramColorKey.switchTrackBlue);

  /// key_switchTrackBlueChecked
  Color get trackBlueChecked =>
      _resolve(TelegramColorKey.switchTrackBlueChecked);

  /// key_switchTrackBlueThumb
  Color get trackBlueThumb => _resolve(TelegramColorKey.switchTrackBlueThumb);

  /// key_switchTrackBlueThumbChecked
  Color get trackBlueThumbChecked =>
      _resolve(TelegramColorKey.switchTrackBlueThumbChecked);

  /// key_switchTrackBlueSelector
  Color get trackBlueSelector =>
      _resolve(TelegramColorKey.switchTrackBlueSelector);

  /// key_switchTrackBlueSelectorChecked
  Color get trackBlueSelectorChecked =>
      _resolve(TelegramColorKey.switchTrackBlueSelectorChecked);

  /// key_switch2Track
  Color get switch2Track => _resolve(TelegramColorKey.switch2Track);

  /// key_switch2TrackChecked
  Color get switch2TrackChecked =>
      _resolve(TelegramColorKey.switch2TrackChecked);
}

/// checkbox* — checkboxes (round and square)
class CheckboxColors {
  const CheckboxColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_checkboxSquareBackground
  Color get squareBackground =>
      _resolve(TelegramColorKey.checkboxSquareBackground);

  /// key_checkboxSquareCheck
  Color get squareCheck => _resolve(TelegramColorKey.checkboxSquareCheck);

  /// key_checkboxSquareUnchecked
  Color get squareUnchecked =>
      _resolve(TelegramColorKey.checkboxSquareUnchecked);

  /// key_checkboxSquareDisabled
  Color get squareDisabled => _resolve(TelegramColorKey.checkboxSquareDisabled);

  /// key_checkbox
  Color get checkbox => _resolve(TelegramColorKey.checkbox);

  /// key_checkboxDisabled
  Color get disabled => _resolve(TelegramColorKey.checkboxDisabled);

  /// key_checkboxCheck
  Color get check => _resolve(TelegramColorKey.checkboxCheck);
}

/// featuredStickers_* — featured sticker pack cells
class FeaturedStickersColors {
  const FeaturedStickersColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_featuredStickers_addedIcon
  Color get addedIcon => _resolve(TelegramColorKey.featuredStickers_addedIcon);

  /// key_featuredStickers_buttonProgress
  Color get buttonProgress =>
      _resolve(TelegramColorKey.featuredStickers_buttonProgress);

  /// key_featuredStickers_addButton
  Color get addButton => _resolve(TelegramColorKey.featuredStickers_addButton);

  /// key_featuredStickers_addButton2
  Color get addButton2 =>
      _resolve(TelegramColorKey.featuredStickers_addButton2);

  /// key_featuredStickers_addButtonPressed
  Color get addButtonPressed =>
      _resolve(TelegramColorKey.featuredStickers_addButtonPressed);

  /// key_featuredStickers_removeButtonText
  Color get removeButtonText =>
      _resolve(TelegramColorKey.featuredStickers_removeButtonText);

  /// key_featuredStickers_buttonText
  Color get buttonText =>
      _resolve(TelegramColorKey.featuredStickers_buttonText);

  /// key_featuredStickers_unread
  Color get unread => _resolve(TelegramColorKey.featuredStickers_unread);
}

/// groupcreate_* — group create / invite screens
class GroupCreateColors {
  const GroupCreateColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_groupcreate_hintText
  Color get hintText => _resolve(TelegramColorKey.groupcreate_hintText);

  /// key_groupcreate_cursor
  Color get cursor => _resolve(TelegramColorKey.groupcreate_cursor);

  /// key_groupcreate_sectionShadow
  Color get sectionShadow =>
      _resolve(TelegramColorKey.groupcreate_sectionShadow);

  /// key_groupcreate_sectionText
  Color get sectionText => _resolve(TelegramColorKey.groupcreate_sectionText);

  /// key_groupcreate_spanText
  Color get spanText => _resolve(TelegramColorKey.groupcreate_spanText);

  /// key_groupcreate_spanBackground
  Color get spanBackground =>
      _resolve(TelegramColorKey.groupcreate_spanBackground);

  /// key_groupcreate_spanDelete
  Color get spanDelete => _resolve(TelegramColorKey.groupcreate_spanDelete);
}

/// code_* — syntax highlighting in code blocks
class CodeColors {
  const CodeColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_code_keyword
  Color get keyword => _resolve(TelegramColorKey.code_keyword);

  /// key_code_operator
  Color get codeOperator => _resolve(TelegramColorKey.code_operator);

  /// key_code_constant
  Color get constant => _resolve(TelegramColorKey.code_constant);

  /// key_code_string
  Color get string => _resolve(TelegramColorKey.code_string);

  /// key_code_number
  Color get number => _resolve(TelegramColorKey.code_number);

  /// key_code_comment
  Color get comment => _resolve(TelegramColorKey.code_comment);

  /// key_code_function
  Color get function => _resolve(TelegramColorKey.code_function);
}

/// color_* — peer name color palette
class PeerColors {
  const PeerColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_color_lightblue
  Color get lightblue => _resolve(TelegramColorKey.color_lightblue);

  /// key_color_blue
  Color get blue => _resolve(TelegramColorKey.color_blue);

  /// key_color_green
  Color get green => _resolve(TelegramColorKey.color_green);

  /// key_color_lightgreen
  Color get lightgreen => _resolve(TelegramColorKey.color_lightgreen);

  /// key_color_red
  Color get red => _resolve(TelegramColorKey.color_red);

  /// key_color_orange
  Color get orange => _resolve(TelegramColorKey.color_orange);

  /// key_color_yellow
  Color get yellow => _resolve(TelegramColorKey.color_yellow);

  /// key_color_purple
  Color get purple => _resolve(TelegramColorKey.color_purple);

  /// key_color_cyan
  Color get cyan => _resolve(TelegramColorKey.color_cyan);
}

/// glass_* — liquid glass tab bar and surface tint targets
class GlassColors {
  const GlassColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_glass_defaultIcon
  Color get defaultIcon => _resolve(TelegramColorKey.glass_defaultIcon);

  /// key_glass_defaultText
  Color get defaultText => _resolve(TelegramColorKey.glass_defaultText);

  /// key_glass_targetMainTabs
  Color get targetMainTabs => _resolve(TelegramColorKey.glass_targetMainTabs);

  /// key_glass_targetMainTopPanel
  Color get targetMainTopPanel =>
      _resolve(TelegramColorKey.glass_targetMainTopPanel);

  /// key_glass_tabSelected
  Color get tabSelected => _resolve(TelegramColorKey.glass_tabSelected);

  /// key_glass_tabSelectedText
  Color get tabSelectedText => _resolve(TelegramColorKey.glass_tabSelectedText);

  /// key_glass_tabUnselected
  Color get tabUnselected => _resolve(TelegramColorKey.glass_tabUnselected);
}

/// telegram_color* — the Telegram accent color family
class AccentColors {
  const AccentColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_telegram_color_dialogsLogo
  Color get dialogsLogo =>
      _resolve(TelegramColorKey.telegram_color_dialogsLogo);

  /// key_telegram_color
  Color get telegramColor => _resolve(TelegramColorKey.telegram_color);

  /// key_telegram_color_text
  Color get text => _resolve(TelegramColorKey.telegram_color_text);
}

/// Keys that match no prefix in groups.toml — the generation-stable
/// catch-all (new upstream keys land here before being grouped).
class UngroupedColors {
  const UngroupedColors(this._resolve);

  final TelegramColorResolver _resolve;

  /// key_wallpaperFileOffset
  Color get wallpaperFileOffset =>
      _resolve(TelegramColorKey.wallpaperFileOffset);

  /// key_progressCircle
  Color get progressCircle => _resolve(TelegramColorKey.progressCircle);

  /// key_listSelector
  Color get listSelector => _resolve(TelegramColorKey.listSelector);

  /// key_settings_listSelector
  Color get settingsListSelector =>
      _resolve(TelegramColorKey.settings_listSelector);

  /// key_emptyListPlaceholder
  Color get emptyListPlaceholder =>
      _resolve(TelegramColorKey.emptyListPlaceholder);

  /// key_divider
  Color get divider => _resolve(TelegramColorKey.divider);

  /// key_graySection
  Color get graySection => _resolve(TelegramColorKey.graySection);

  /// key_graySectionText
  Color get graySectionText => _resolve(TelegramColorKey.graySectionText);

  /// key_radioBackground
  Color get radioBackground => _resolve(TelegramColorKey.radioBackground);

  /// key_radioBackgroundChecked
  Color get radioBackgroundChecked =>
      _resolve(TelegramColorKey.radioBackgroundChecked);

  /// key_fastScrollActive
  Color get fastScrollActive => _resolve(TelegramColorKey.fastScrollActive);

  /// key_fastScrollInactive
  Color get fastScrollInactive => _resolve(TelegramColorKey.fastScrollInactive);

  /// key_fastScrollText
  Color get fastScrollText => _resolve(TelegramColorKey.fastScrollText);

  /// key_pollCreateIcons
  Color get pollCreateIcons => _resolve(TelegramColorKey.pollCreateIcons);

  /// key_text_RedRegular
  Color get textRedRegular => _resolve(TelegramColorKey.text_RedRegular);

  /// key_text_RedBold
  Color get textRedBold => _resolve(TelegramColorKey.text_RedBold);

  /// key_fill_RedNormal
  Color get fillRedNormal => _resolve(TelegramColorKey.fill_RedNormal);

  /// key_fill_RedDark
  Color get fillRedDark => _resolve(TelegramColorKey.fill_RedDark);

  /// key_returnToCallBackground
  Color get returnToCallBackground =>
      _resolve(TelegramColorKey.returnToCallBackground);

  /// key_returnToCallMutedBackground
  Color get returnToCallMutedBackground =>
      _resolve(TelegramColorKey.returnToCallMutedBackground);

  /// key_returnToCallText
  Color get returnToCallText => _resolve(TelegramColorKey.returnToCallText);

  /// key_contextProgressInner1
  Color get contextProgressInner1 =>
      _resolve(TelegramColorKey.contextProgressInner1);

  /// key_contextProgressOuter1
  Color get contextProgressOuter1 =>
      _resolve(TelegramColorKey.contextProgressOuter1);

  /// key_contextProgressInner2
  Color get contextProgressInner2 =>
      _resolve(TelegramColorKey.contextProgressInner2);

  /// key_contextProgressOuter2
  Color get contextProgressOuter2 =>
      _resolve(TelegramColorKey.contextProgressOuter2);

  /// key_contextProgressInner3
  Color get contextProgressInner3 =>
      _resolve(TelegramColorKey.contextProgressInner3);

  /// key_contextProgressOuter3
  Color get contextProgressOuter3 =>
      _resolve(TelegramColorKey.contextProgressOuter3);

  /// key_contextProgressInner4
  Color get contextProgressInner4 =>
      _resolve(TelegramColorKey.contextProgressInner4);

  /// key_contextProgressOuter4
  Color get contextProgressOuter4 =>
      _resolve(TelegramColorKey.contextProgressOuter4);

  /// key_buttonNeutral
  Color get buttonNeutral => _resolve(TelegramColorKey.buttonNeutral);

  /// key_buttonNeutralText
  Color get buttonNeutralText => _resolve(TelegramColorKey.buttonNeutralText);

  /// key_table_background
  Color get tableBackground => _resolve(TelegramColorKey.table_background);

  /// key_table_border
  Color get tableBorder => _resolve(TelegramColorKey.table_border);

  /// key_passport_authorizeBackground
  Color get passportAuthorizeBackground =>
      _resolve(TelegramColorKey.passport_authorizeBackground);

  /// key_passport_authorizeBackgroundSelected
  Color get passportAuthorizeBackgroundSelected =>
      _resolve(TelegramColorKey.passport_authorizeBackgroundSelected);

  /// key_passport_authorizeText
  Color get passportAuthorizeText =>
      _resolve(TelegramColorKey.passport_authorizeText);

  /// key_sharedMedia_startStopLoadIcon
  Color get sharedMediaStartStopLoadIcon =>
      _resolve(TelegramColorKey.sharedMedia_startStopLoadIcon);

  /// key_sharedMedia_linkPlaceholder
  Color get sharedMediaLinkPlaceholder =>
      _resolve(TelegramColorKey.sharedMedia_linkPlaceholder);

  /// key_sharedMedia_linkPlaceholderText
  Color get sharedMediaLinkPlaceholderText =>
      _resolve(TelegramColorKey.sharedMedia_linkPlaceholderText);

  /// key_sharedMedia_photoPlaceholder
  Color get sharedMediaPhotoPlaceholder =>
      _resolve(TelegramColorKey.sharedMedia_photoPlaceholder);

  /// key_stickers_menu
  Color get stickersMenu => _resolve(TelegramColorKey.stickers_menu);

  /// key_stickers_menuSelector
  Color get stickersMenuSelector =>
      _resolve(TelegramColorKey.stickers_menuSelector);

  /// key_changephoneinfo_image2
  Color get changephoneinfoImage2 =>
      _resolve(TelegramColorKey.changephoneinfo_image2);

  /// key_contacts_inviteBackground
  Color get contactsInviteBackground =>
      _resolve(TelegramColorKey.contacts_inviteBackground);

  /// key_contacts_inviteText
  Color get contactsInviteText =>
      _resolve(TelegramColorKey.contacts_inviteText);

  /// key_login_progressInner
  Color get loginProgressInner =>
      _resolve(TelegramColorKey.login_progressInner);

  /// key_login_progressOuter
  Color get loginProgressOuter =>
      _resolve(TelegramColorKey.login_progressOuter);

  /// key_picker_enabledButton
  Color get pickerEnabledButton =>
      _resolve(TelegramColorKey.picker_enabledButton);

  /// key_picker_disabledButton
  Color get pickerDisabledButton =>
      _resolve(TelegramColorKey.picker_disabledButton);

  /// key_picker_badge
  Color get pickerBadge => _resolve(TelegramColorKey.picker_badge);

  /// key_picker_badgeText
  Color get pickerBadgeText => _resolve(TelegramColorKey.picker_badgeText);

  /// key_files_folderIcon
  Color get filesFolderIcon => _resolve(TelegramColorKey.files_folderIcon);

  /// key_files_folderIconBackground
  Color get filesFolderIconBackground =>
      _resolve(TelegramColorKey.files_folderIconBackground);

  /// key_files_iconText
  Color get filesIconText => _resolve(TelegramColorKey.files_iconText);

  /// key_sessions_devicesImage
  Color get sessionsDevicesImage =>
      _resolve(TelegramColorKey.sessions_devicesImage);

  /// key_calls_callReceivedGreenIcon
  Color get callsCallReceivedGreenIcon =>
      _resolve(TelegramColorKey.calls_callReceivedGreenIcon);

  /// key_calls_callReceivedRedIcon
  Color get callsCallReceivedRedIcon =>
      _resolve(TelegramColorKey.calls_callReceivedRedIcon);

  /// key_undo_background
  Color get undoBackground => _resolve(TelegramColorKey.undo_background);

  /// key_undo_cancelColor
  Color get undoCancelColor => _resolve(TelegramColorKey.undo_cancelColor);

  /// key_undo_infoColor
  Color get undoInfoColor => _resolve(TelegramColorKey.undo_infoColor);

  /// key_sheet_scrollUp
  Color get sheetScrollUp => _resolve(TelegramColorKey.sheet_scrollUp);

  /// key_sheet_other
  Color get sheetOther => _resolve(TelegramColorKey.sheet_other);

  /// key_bot_loadingIcon
  Color get botLoadingIcon => _resolve(TelegramColorKey.bot_loadingIcon);

  /// key_gift_ribbon
  Color get giftRibbon => _resolve(TelegramColorKey.gift_ribbon);

  /// key_gift_ribbon_soldout
  Color get giftRibbonSoldout => _resolve(TelegramColorKey.gift_ribbon_soldout);

  /// key_reactionStarSelector
  Color get reactionStarSelector =>
      _resolve(TelegramColorKey.reactionStarSelector);

  /// key_topics_unreadCounter
  Color get topicsUnreadCounter =>
      _resolve(TelegramColorKey.topics_unreadCounter);

  /// key_topics_unreadCounterMuted
  Color get topicsUnreadCounterMuted =>
      _resolve(TelegramColorKey.topics_unreadCounterMuted);

  /// key_starsGradient1
  Color get starsGradient1 => _resolve(TelegramColorKey.starsGradient1);

  /// key_starsGradient2
  Color get starsGradient2 => _resolve(TelegramColorKey.starsGradient2);

  /// key_iv_background
  Color get ivBackground => _resolve(TelegramColorKey.iv_background);

  /// key_iv_backgroundGray
  Color get ivBackgroundGray => _resolve(TelegramColorKey.iv_backgroundGray);

  /// key_iv_ab_progress
  Color get ivAbProgress => _resolve(TelegramColorKey.iv_ab_progress);

  /// key_iv_navigationBackground
  Color get ivNavigationBackground =>
      _resolve(TelegramColorKey.iv_navigationBackground);

  /// key_share_linkText
  Color get shareLinkText => _resolve(TelegramColorKey.share_linkText);

  /// key_share_linkBackground
  Color get shareLinkBackground =>
      _resolve(TelegramColorKey.share_linkBackground);

  /// key_share_icon
  Color get shareIcon => _resolve(TelegramColorKey.share_icon);

  /// key_botKeyboard_button_primary
  Color get botKeyboardButtonPrimary =>
      _resolve(TelegramColorKey.botKeyboard_button_primary);

  /// key_botKeyboard_button_danger
  Color get botKeyboardButtonDanger =>
      _resolve(TelegramColorKey.botKeyboard_button_danger);

  /// key_botKeyboard_button_success
  Color get botKeyboardButtonSuccess =>
      _resolve(TelegramColorKey.botKeyboard_button_success);
}
