/// `package:lottie` adapter for `telegram_ui`: [LottieTabAnimation]
/// implements the `TabAnimationController` playback contract of the glass
/// tab bar (see `telegram_ui`'s `tab_icon.dart`), backed by a
/// `LottieComposition` and an [AnimationController].
library;

export 'src/lottie_tab_animation.dart';
